# Bounded fresh-driver evidence

Copied unchanged from completed Prometheus job300308, run
`/lustreFS/data/superworld/ckontzias/thesis/experiments/e18-fresh-integration/run-20260905-a9d1c265`.
Five INTEGRATION.json outputs and adjacent seals. Read only after terminal
success and checksum validation. No reward/coverage/success table is included.

Each file retains coefficient values, real-input dependency checks, input/model
hashes and fixed short driver lifecycle traces. These are exposed-record
engineering artifacts, not a confirmation dataset. Three R1 episodes are reused.

SOURCE-MANIFEST.sha256 identifies the executed nine-file integration snapshot;
it is not a checksum list for this evidence directory. Replay the independent
CPU verifier with `python verify_e18_fresh_integration.py e18-fresh-integration-evidence`.
