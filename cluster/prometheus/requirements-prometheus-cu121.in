# Direct research/runtime pins for the Prometheus Hi-LeWM environment.
# The complete transitive lock is written after installation to
# manifests/requirements-prometheus-cu121-freeze.txt on Lustre.

# Install the training and HDF5 surfaces, then explicitly pin only the two
# environments in this study (PushT and OGBench Cube). The upstream `env`
# extra also installs unrelated Atari, Craftax, MiniGrid, and robotics stacks.
stable-worldmodel[train,format]==0.1.1
stable-pretraining==0.1.8
transformers==4.57.6
numpy==2.2.6
gymnasium==1.3.0
h5py==3.16.0
ogbench==1.2.1
pymunk==7.3.0
pygame==2.6.1
shapely==2.1.2
opencv-python-headless==5.0.0.93
hydra-core==1.3.4
hydra-submitit-launcher==1.2.0
pytorch-lightning==2.6.5
wandb==0.28.1
pytest==9.1.1
