# ACVM1 R2 launched; successful fit retained; controller acceptance fault

The R2 source/authority/SSD preparation was committed at `fa5ba4c34939a07c1d7702822d68f7822a625b4c` and its exact remote branch hash verified before stage. Cluster stage was invoked once; source manifest `068dff5df9b541bae4c3db53953704c09e2239015b1c107afbfe127a5541a14e` and enabled approval `e2b1bb3a4e7970910abc9625035df8f9dae099bd23f6b2d00f69fe30bb71e0bf` matched. The R2 controller launched once at Unix1790262035.9335647, PID2845226/start_ticks901892485, and submitted `fit-pair1-joint` job304593 at Unix1790262039.2811272.

Independent exact-ID Slurm accounting observed job304593 `COMPLETED|0:0`, 11 allocation-wall seconds, 4 CPUs/8GiB, gpu01, defq/normal, no GPU. The R2 worker passed its authenticated import path and produced sealed successful fitting output; neither its failed R1 output nor the two earlier failed allocations were recomputed. At the first acceptance event, after terminal charging and worker acceptance checks, the controller stopped on `TypeError("dict() got multiple values for keyword argument 'unix'")`: the inherited campaign event helper supplied its own `unix` while the acceptance receipt also supplied `unix`. The R2 STOP is SHA256 `6404de638c2ea7fcd9fa36f25bd7254be73d3cd97288a5917d0b39d79a205184`. The exact controller process exited; no model freeze, technical evaluation tranche or scientific results exist.

The user-approved cumulative CPU ceiling remains36,008 allocation-wall seconds. Accounted CPU charges are0+8+11=**19** seconds; GPU charges are0. One of the four new fits is successful and must be authenticated and reused. Three fits, one CPU analysis and8,192 evaluation tasks remain; no scientific task is to be added. R2 must not be restarted or patched. A new exclusive control-only continuation must first reconcile the exact R2 sealed output and all three allocations, preserve all historical STOPs and acceptance-fault evidence, then skip the successful fit. This fault is control bookkeeping, not grounds to fit again, change scientific choices or inspect partial efficacy.
