EXECUTE ACV MECHANISM REPLICATION runtime-v2 —
fixed512-source developmental study under the user's standing delegation.

This is explicit execution authorization for the corrected package below.
It is not a newly obtained direct user signature.

Runtime source/export commit:
d1710a059b047a37b4cd6419b087543c7ae0f6fa

Reviewed delivery publication:
7fa8ab412d0c24f9b0afc7854980733fb8dcc24e

No additional general approval or preparation cycle is required for
this exact execution. Preserve all earlier packages and receipts.

1. EXACT PACKAGE AND NEW APPROVAL

Source-manifest SHA256:
9f91156fd11a5d54172e8cec79d37ca50199c132780b5aee3636f34b0dd2e916

Source export:
4,894,720 bytes /76 regular members
SHA256:
ba58cff72d12322eb98cd40cd1707df437ca769e824da2d0a5599ccb6b8e44e2

Preserved disabled-approval SHA256:
2e9afd7dee838c4b14ada69215dc151844908fd10f0e3c8bc6fbbe389d9f4c70

Exact approved run:
/lustreFS/data/superworld/ckontzias/thesis/experiments/active-counterfactual-mechanism-replication-v1/run-9f91156fd11a5d54

Create a SEPARATE enabled approval under the existing strict schema.
Bind it to this instruction, the exact manifest and all unchanged
template bindings/caps. Preserve the false template unchanged.

Record the instruction as authorization under the existing user
delegation—not a forged direct user signature. Record the enabled
approval's actual hash and preserve its exact staged copy.

Deploy the byte-authenticated source tar with its repository layout.
Do not reconstruct it through a newline-normalized Git checkout.
Preserve the documented historical initializer representation.

2. PRELAUNCH RECONCILIATION AND EXCLUSIVE LAUNCH

Before staging:
- Reconcile the recorded role-ledger inventory and registry metadata
  against intervening assignments.
- Require the same512 ordered identities and existing exclusions.
- Verify the designated SSD identity,40GB free-space requirement,
  corrected source-package copy and receipt.
- Confirm no existing ACVM1 launch, allocation, ambiguous submission,
  controller or intended source/run/control namespace.

Use the existing configured account, SSH route, hostPython3.9 control
plane, Slurm binaries and pinned worker runtime.

An identity conflict or existing namespace stops launch. Do not replace
sources, rename around an unresolved attempt, or bypass an interlock.

Use the prepared transport stage/launch and serial dispatcher.
Preserve exact controller PID/start-time identity and submission receipts.
Do not restart historical controllers or monitors.

No extra research preflight allocation, model-only probe or separate
simulator pilot is authorized.

3. FITTING, REUSE AND EVALUATION SCOPE

Reuse the authenticated original pair94011/94012 without refitting.

Run exactly four new CPU fits:
- Joint94411.
- Ordinary94412.
- Joint94421.
- Ordinary94422.

Each receives exactly192 updates on the unchanged saved64-source
fitting dataset, using the accepted preprocessing, architecture, losses,
optimizer and shuffle rules. The saved16 validation sources remain
reporting-only.

No early stopping, seed selection, extra epoch, ensemble, calibration
sweep or checkpoint selection. Retain final-update checkpoints and
complete fit histories.

Freeze all six model identities and preprocessing before opening any
new evaluation reference.

Execute the exact existing grid:
five learned policies × three fixed model pairs ×512 sources,
plus one early-replanning episode per source.

This is8,192 actual evaluation episodes, not three independent source
cohorts. Early replanning is executed once/source and reused only
algebraically in the fixed paired summaries.

Keep:
static, committed_feedback, no_update, active, ordinary, early-replan.

Preserve H75,150 physical actions, one initial five-action prefix/
ten-action continuation opportunity, the unchanged baseline tail,
maximum4×4 tree,128 integration samples, all planner seeds, precisions,
projections, tie rules, terminal handling and baseline guards.

No additional action-proposer training, physics variation, repeated
active intervention, new control or source substitution.

4. INCLUDED FIRST-SOURCE TECHNICAL GATE

The first source's16 episodes in the frozen grid are the included
technical tranche and remain part of the final study.

All16 must complete and pass before source2 is submitted:
- Exact source, goal, hardware and allocation association.
- Frozen model identities.
- Exact initial tree/action/predicted-feature pairing.
- Authenticated initial/goal metadata and saved-evidence readback.
- Same-source/exact-action prefix-history coupling.
- Native endpoint, action clock, termination and full-budget checks.
- Worker seals and all resource limits.

Evaluate this gate from technical projections and resource receipts.
Do not inspect success rates or model rankings to decide continuation.

If it passes, continue the already approved remainder without another
general permission checkpoint.

Every later worker retains its individual checks. Final independent
analysis must reconstruct prefix coupling from actual saved arrays
for every source, not merely trust the first-source gate or digest fields.

5. COMPUTE AND STORAGE LIMITS

Maximum research allocations:
8,192 GPU evaluations + four CPU fits + one CPU analysis =8,197.

GPU workers:
-Exactly one NVIDIA RTX6000 Ada Generation at a time.
-Existing gpu09/gpu09.cluster association and exact Slurm job identity.
-4CPUs/24GiB.
-300-second allocation limit;240-second work boundary and60-second
 preservation margin.
-2,457,600 aggregate GPU-allocation seconds.

CPU fitting/analysis:
-Five allocations, each4CPUs/8GiB/7,200seconds.
-36,000 aggregate allocation-wall seconds.
-No GPU access.

Charge failures, cancellations and timeouts. Queue time is recorded
separately and excluded from GPU-allocation charges.

Preserve full-future reservations before dispatch. Unused headroom
does not authorize another attempt or a larger study.

Storage:
-2,000,000 bytes per complete evaluation worker.
-Existing100,000,000-byte CPU-worker limits.
-1,000,000,000 bytes combined retained-input/source/model/control/
 analysis allowance, including the declared logs and metadata.
-18,000,000,000 live bytes.
-18,500,000,000 archive bytes.
-80,000,000,000 inclusive preservation allowance.
-All existing log/member limits and designated-SSD requirements.

Count hidden files, failed outputs, source copies, metadata, seals,
archive overhead and retained transfer partials. Do not delete evidence
or exclude another directory to make accounting pass.

Retain the existing separately accounted preservation host limits:
7,200seconds archive/authentication;
14,400seconds one transfer plus verification;
7,200seconds post-preservation reporting.
These are not extra GPU or research-job allowances.

6. FAIL-STOP AND RECOVERY

No automatic retry, requeue, resume, replacement task/source or
resource expansion is authorized.

On a technical fault:
-stop new dispatch;
-preserve completed outputs, partials and raw fault evidence;
-reconcile submitted, terminal, live and ambiguous allocations;
-retain every charge;
-return one scoped report.

Do not rerun a successfully completed scientific task to repair
controller acceptance or preservation.

The finalizer's explicit-resolution facility is not permission to
self-authorize an unresolved fault. Any recovery outside this fixed
successful path requires a separately scoped direction.

Unfavorable success rates, weak fitted models, sparse contrasts or
an absent positive-mechanism flag are scientific outcomes, not faults.

7. FROZEN ANALYSIS AND INFORMATION BARRIER

Retain the seven specified contrasts, exact source/seed pairing,
10,000 whole-source bootstrap resamples with seed94431, per-pair
summaries, nominal adjusted intervals and conservative Hoeffding bounds.

Primary mechanism contrasts remain:
-active minus no_update;
-active minus committed_feedback.

Report the operational interaction and external comparisons, including
early replanning, regardless of sign.

No old32 pooling, favorable subset, best-seed selection or new
inferential procedure. No expansion until an interval passes.

The Hoeffding-positive flag is an additional stringent certificate.
Its absence is not an automatic scientific-failure verdict.
Nominal bootstrap intervals are not finite-sample guarantees.
Interpret the fixed five-point practical effect alongside uncertainty
and costs; do not turn it into a new outcome-dependent gate.

Keep scientific aggregates unread by the research workflow until all
task acceptance, final reconciliation and external preservation pass.
Automated verification may compute the quantities required by the
frozen pipeline; no partial efficacy-based decisions follow.

8. FINAL PRESERVATION AND RETURN

Independently accept all8,197 task identities and8,192 episode outputs,
including exact six-model provenance, pairing, endpoints and charges.

Create one final new-study-only archive with the complete source,
approvals, reused-input provenance, fits, evaluations, analysis,
controller records, accounting and any retained failure history.

Verify its whole-file hash and every expected member on the designated
THESIS_SSD. No historical-study rearchive or bulk retransfer.

One transfer attempt is authorized under the existing deadline/byte cap.
Preserve partials on failure. No silent retry, overwrite, fallback disk,
fake acknowledgment or recomputation.

Publish launch and final records outside the immutable execution closure.
Verify full remote commit identities.

Preserve all historical outcomes, ACV0, AV0, AV1's unlaunched status,
reserved/protected payload boundaries and the E12 drafts.
No model promotion, external benchmark or next study launches automatically.

Return the launch/approval identities and included-tranche technical
status when available, or a concrete fault report.

After complete preservation, return all512 source outcomes/effects,
all fixed model-pair results, seven contrasts, decision/guard records,
full costs and verified backup scope.