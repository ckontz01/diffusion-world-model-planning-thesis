# ACV mechanism replication runtime-v2 execution authority

24 September 2026. The supplied `EXECUTE ACV MECHANISM REPLICATION runtime-v2` instruction explicitly authorizes this exact fixed512-source developmental campaign under the user's standing delegation. This is recorded delegated execution direction, **not a newly obtained direct user signature**. The original instruction is retained byte-for-byte in `AUTHORIZATION-INSTRUCTION.md`; its SHA256 is `b97743d0b3599dc1099377b515f5c8a301333a2df8a6c1aff695fa31a40780da`.

The immutable runtime/export is commit `d1710a059b047a37b4cd6419b087543c7ae0f6fa`, delivered at `7fa8ab412d0c24f9b0afc7854980733fb8dcc24e`. Manifest `9f91156fd11a5d54172e8cec79d37ca50199c132780b5aee3636f34b0dd2e916` and export `ba58cff72d12322eb98cd40cd1707df437ca769e824da2d0a5599ccb6b8e44e2` (4,894,720 bytes /76 regular members) remain unchanged. Deployment uses the authenticated tar and retains the historical initializer's exact representation, not a normalized Git reconstruction.

`EXECUTION-APPROVAL.json` here is a **separate** enabled capability. Only `authorized` and `instruction` differ from the frozen false template. Its actual hash is in `AUTHORIZATION.json`. The source template remains false at `2e9afd7dee838c4b14ada69215dc151844908fd10f0e3c8bc6fbbe389d9f4c70`. All bindings, caps, no-retry rule and exact run remain unchanged. The enabled approval's exact bytes will be staged separately in the new control directory; the tar retains the false template.

## Fresh checks before staging

`PRELAUNCH.json` and `PRELAUNCH-REMOTE.json` document fresh repository role-ledger inventory, all recorded exclusion roles, authenticated registry identity projection and exact512 order/identities. No new reference payload or scientific outcome was decoded. Reused model/data files and pinned control/worker runtime were byte-hashed only, not loaded or executed. No intervening recorded role conflicts, ACVM1 namespace, matching allocation or controller was found. Scope is the recorded role inventory plus authenticated registry, not an assertion about unrecorded external activity.

Native Windows confirmed THESIS_SSD volume `0a2f1ba9-0000-0000-0000-100000000000`, 322,749,501,440 free bytes, and reauthenticated the existing corrected source copy's whole file and all76 members. No historical archive or source package was recreated or retransferred.

## Execution boundaries

Exactly four new CPU fits (94411/94412 and94421/94422,192 updates each), reused94011/94012,8192 GPU evaluations and one CPU analysis:8197 unique task allocations maximum. Six-model/preprocessing freeze precedes new evaluation access. First-source16 episodes are included main tasks and must pass the fixed technical gate before source2; no efficacy criterion or further general permission checkpoint is added.

GPU ceiling2,457,600 allocation-seconds, serial one exact RTX6000 Ada on gpu09/gpu09.cluster; CPU-stage ceiling36,000 allocation-wall seconds; existing per-task limits and18GB live/18.5GB archive/80GB inclusive caps remain frozen. All future reservations and failed charges count. Scientific aggregates stay unread until complete independent acceptance and designated-SSD whole/member preservation.

No automatic retry, requeue, resumption, replacement, new source, changed science or resource expansion. A fault requires preserved evidence, full allocation reconciliation and one scoped report; historical ACV0 technical-repair authority does not override this instruction. No historical controller or monitor is restarted. E12, old results and AV1's unlaunched status remain unchanged.
