# Scoped technical fault: CPU bootstrap interpreter unavailable

**ACVM1 runtime-v2 is stopped after one failed CPU allocation. No recovery has been attempted or authorized by this report.** The included16-episode technical gate was not reached.

## Concrete cause

Task `fit-pair1-joint`, seed94411, Slurm allocation304589, ran on CPU partition `defq`, node `gpu02`, account `superworld`, QOS `normal`,4CPUs/8GiB,120-minute limit and no GPU allocation. It failed with `FAILED|127:0` before the host supervisor/fitting worker started. The exact96-byte Slurm error is:

```text
/var/spool/slurmd/job304589/slurm_script: line 4: /usr/bin/python3.9: No such file or directory
```

The frozen `run_worker.sh` line4 invokes `/usr/bin/python3.9`. That binary and its pinned hash were verified on the login/control host, not on every possible `defq` worker. This allocation demonstrates that the control-host interpreter assumption does not hold on `gpu02`. No model, optimizer, predictor, simulator or scientific outcome caused this failure. Availability on other CPU or GPU nodes is not established by this observation; no extra node probe or allocation was made.

## Fail-stop and independent reconciliation

The controller recorded terminal accounting before acceptance. `acceptance.worker` rejected the nonzero exit; `STOP.json` records `ValueError('Successful exit required')`, Unix1790258419.539252. STOP SHA256 is `237cd6138603a1d72a2f04f6c1d623746f03e3a40da5c7b89c1dd667cba9458b`. Exact controller PID2760213/start_ticks901529006 had exited at the final reconciliation.

Independent `sacct` and `squeue` for the exact sole submitted ID304589 confirm one terminal failed allocation, no live allocation, no ambiguous submission and no missing claim-to-submission association. The ledger has one claim, one submission, one terminal event and zero acceptances. No later task was claimed or submitted.

Slurm reports `ElapsedRaw=0`: retained charges are **0 GPU allocation-seconds and0 CPU-stage allocation-wall seconds**, at the scheduler's whole-second resolution. This is one charged/accounted failed attempt, not permission to erase the attempt or assert that absolutely no machine time elapsed. No CPU worker output directory or partial model was created. No new evaluation reference was opened, no GPU used and no scientific aggregate decoded.

All seven original reused checkpoint/data/preprocessing/fit-provenance files remain sealed. Reused seal SHA256: `f975fbd2f856099d28e362a934f30e7807d19cbaf5b3150ee17efc71da02ee4a`. Their verification read bytes/hashes only, not predictions or outcomes. No original pair was refitted.

The frozen future-work reservation at the failed claim was2,457,600 GPU seconds plus36,000 CPU-stage allocation-wall seconds. Actual retained bytes at reconciliation were4,762,069 source,19,816 control and6,976,685 run bytes. The source count includes one generated Python bytecode cache as well as the76 exported files; it is retained and counted, not deleted. These are current logical bytes, not a final archive or full preservation claim.

## Evidence preserved and publication scope

`FAULT-TECHNICAL.json` contains the complete controller STOP/traceback, raw submission/ledger/scheduler records, independent accounting, exact error, log hashes/lengths, reused seal and process/byte observations. `RECEIPTS.json` binds the authorization, fresh prelaunch reconciliation, stage, staged readback, launch and fault observations. Frozen source, tar, false template, enabled staged approval and all cluster files remain unchanged.

The job's `.slurm.out` contains only an unrelated site account-balance footer. Its original bytes remain intact on the cluster and in the untracked local `FAULT-RECONCILIATION.json`; publication includes its hash/length, not third-party account details. The raw local receipt's hash is bound in the published technical receipt. Nothing was deleted or rewritten to achieve this publication projection.

The existing corrected small source export remains verified on the designated SSD. No final campaign archive or backup was attempted, and no historical archive was recreated or transferred.

## Boundary for any separately scoped recovery

The current instruction explicitly prohibits automatic retry/resumption/replacement and overrides earlier ACV0 repair authority for this study. No live source patch, dependency/permission change, alternate namespace, resubmission, controller restart, scientific change or monitor restart was performed.

A future direction must specifically authorize a minimal worker-bootstrap correction using an available, authenticated interpreter/container path (without changing the accepted scientific/runtime settings), appropriate synthetic/interface validation, a separately frozen recovery package, and at most the explicitly enumerated replacement of this failed task plus the never-submitted remainder. CPU node placement versus a portable bootstrap is a material recovery choice, not silently decided here. Preserve this failed attempt and all caps/reservations.

Because all8197 logical tasks still require successful execution, completing them after this failed allocation would require **8198 total attempts**. That exceeds the current8197-attempt authorization even though the reported elapsed charge is0. Any recovery instruction must explicitly reconcile the one additional attempt; spare compute headroom alone is not authorization. No successful task needs recomputation. No inference about efficacy or the future technical tranche is available.
