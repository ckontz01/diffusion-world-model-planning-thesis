# Approved launch after host-only dispatcher repair

The user authorized the small dispatcher repair and replacement CPU preflight,
and clarified that similarly small technical fixes do not require another
approval question. This does not authorize scientific changes, outcome-based
reruns, tolerance relaxation, silent retries or resource-envelope expansion.

## Preserved scientific identity and narrow repair

Approved experiment commit: `dd0d2bc716e73d49f5d7cf5acf298adc34573032`.
Repair commit: `2f7c941cba546aee497df20dc96bdaa87cf8f63a`.

Only scheduler validation imports were separated into the standard-library-only
`single_anchor_ranking_host.py`. The dispatcher still verifies the source,
approval, protocol, path boundaries and full resource reservations. A regression
test compares the approval condition AST directly against the unchanged worker.
Four host tests cover imports, approval rejection, source/path integrity and
limits. Worker, solver, initializer, decoder, analysis, protocol and all
scientific settings remain byte-identical to the approved Git source. No
dependency was installed, upgraded or silently substituted.

Failed preflight 301088, its source snapshot, logs, accounting and external
backup remain preserved. The new snapshot does not overwrite them.

## New frozen package and successful CPU gate

- Snapshot: `/lustreFS/data/superworld/ckontzias/thesis/snapshots/single-anchor-ranking-20260914-ba2eb02b2e860648`.
- Source manifest: `ba2eb02b2e860648a0642c39cb97f3409ab9cb7dcc975a4110b5e2db05297704`; all 43 entries verified.
- Unchanged protocol: `43830bd18fcf8cf3d986cbd71d5bb8d5df0dee8fd7c88356c5070b5d76c8a1ae`.
- Source archive: `2b6b55266630dcc8744a6cc04b076b5e0cd36e1cce66e2998997b1671e50f6ff`.
- Bound approval: `EXECUTION-APPROVAL-ba2eb02b.json`, copied to the experiment's
  dedicated staging directory on Prometheus.

Replacement CPU preflight **301090** completed `0:0` in 7 seconds, requesting
2 CPUs / 4 GiB / 10 minutes in `defq`, QoS `normal`, account `superworld`.
Both adjacent report seals were validated before accepting the gate:

- Actual host `/usr/bin/python3` 3.6.8: dispatcher import and **4 host tests pass**;
  report SHA-256 `faa06fa08eb809acfcdc9595b8b6e0eb9b132f0ec3c53ea3d680f0fc3e482c74`.
- Pinned container Python 3.11.10, NumPy 2.2.6, torch 2.5.1+cu121: actual runtime
  imports and **22 tests pass** (18 original + 4 host tests); report SHA-256
  `780d836ddaff9e6caaa1752957aab192c5f3d54cc0b401aefafebfae87997a6a`.

Neither CPU preflight accessed a reference payload, constructed a model or
executed physics. The replacement consumed 14 allocated CPU-seconds and 4.753
observed CPU-seconds. Including preserved failure 301088, the CPU preflight
allocations total 14 wall-seconds / 28 allocated CPU-seconds. Slurm batch MaxRSS
was 3,464 KiB for each short job; this sampled step statistic is not a reliable
total container/process-tree peak or a justification to reduce requested RAM.

## Serial execution started

Run root: `/lustreFS/data/superworld/ckontzias/thesis/experiments/single-anchor-ranking-20260914/run-ba2eb02b`.

The verified host dispatcher runs as PID **1370960** on controller1, with its
log in `staging/single-anchor-ranking-20260914/dispatcher-ba2eb02b.log`.
`DISPATCH.jsonl` records reservations, submissions and actual charges. Do not
start a duplicate controller or silently restart an interrupted one.

- Pilot reference 1269 / repeat 0: **301091**, completed `0:0`, charged **46 GPU
  allocation seconds**. Requested 1 A6000 / 4 CPUs / 24 GiB / 15 minutes.
- Pilot reference 1269 / repeat 1: **301092**, submitted next. Its runner checks
  every fresh artifact array against the first repeat before successful exit.

The pilot is inside the fixed 64-job plan; it is not extra experimentation.
After its two successful technical passes, the same dispatcher continues the
remaining 62 jobs automatically. No partial performance result was inspected
when deciding whether to proceed. This file is a launch-time record, not a final
job/accounting snapshot; obtain subsequent IDs/states from scheduler metadata.

All approved caps remain enforced: serial one A6000, 900 seconds/job, full next
900-second reservation, 14,400 aggregate GPU-allocation seconds including failed
jobs, 2,000,000,000-byte run watermark, 64,000,000-byte dispatch reserve and 64 MiB
per-file cap. A failed job stops dispatch and remains charged. No automatic retry.

Heartbeat `monitor-single-anchor-ranking` is active every 20 minutes in this
task, for meaningful changes, failure or completion only. It is a follow-up to
the actual host dispatcher, not a second scheduler or an execution permission.
Before all 64 successful jobs, only scheduler/dispatch metadata, exit codes,
existence, bytes and checksums may be inspected. Exact failed technical logs may
be read only after identifying that failed job. Final sealed analysis and backup
remain pending; no success-effect estimate is available at launch.

## Storage and history

The 6,000,000,000-byte non-sparse backup reservation remains on healthy external
THESIS_SSD. New source, approval and successful preflight evidence are backed up
at `D:/THESIS-BACKUPS/single-anchor-ranking-20260914/launch-evidence-301090.tar`,
with matching cluster/external SHA-256
`d5771f78015fbffa04fc277457c7105d729d3858d9d0527e11176ac637f357c7`.
This backup covers launch evidence only; experiment outputs will be backed up
and accounted separately after execution. Historical decisions, models, completed
diagnostic artifacts and the three E12 drafts remain unchanged. No bulk C: data.
