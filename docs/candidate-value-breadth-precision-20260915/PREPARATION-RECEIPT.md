# CVL-BP1 preparation receipt — 15 September 2026

Prepared on branch `candidate-value-breadth-precision-preparation-20260915`,
starting from accepted result `442c9659a8f8f99e81e492cb9258488b60e12666`.
No historical source/result/checkpoint was amended. Only the new preparation
files and a README history addition are included in its publication.

## Actual completed checks

- Identifier-only projection: 1,600 distinct registered source keys, 192
  previously allocated sources excluded, 1,408 eligible, 96 new training and
  32 new evaluator-held-out development sources assigned, 1,280 unallocated
  within this lineage. Zero reference payloads or outcome fields decoded.
- **26/26 focused tests passed** in the repository (3.238s) and **26/26** in
  the exported source package (3.198s). Timings are unittest execution, not
  shell startup/package I/O. No optimizer steps or learned-model/physics calls.
- Synthetic coverage includes exact C-bank reuse and fatal bank mutation,
  availability preservation, two/four binary draws and paired zero targets,
  role exclusions, hierarchical weighting, seed-output distinction, matched
  update generation, fixed ensemble arithmetic, four-draw metrics/ties,
  all-model/all-reference reporting, pre-evaluation freeze, fail-closed approval,
  Slurm argument generation, cost counts, source packaging and storage races.
- Pure infrastructure function bodies match the accepted repaired dispatcher;
  they are supplied separately from the older immutable scientific closure.
- `bash -n` passed for the new wrapper. `git diff --check` passed.
- The real accepted source closure was copied **source-only** to the external
  SSD, and all 62 inherited entries were preserved byte-for-byte. The new
  79-entry manifest (80 files including itself; 868,687 bytes) verified before
  and after exported tests. No permission change or remote experiment occurred.
- The three unrelated E12 draft paths remain untracked and untouched in
  `/home/chris/thesis`. Historical decisions remain `stop_no_ranking_promise`
  and the recorded training-only nominee `original_relative`; continuation
  remains the baseline.

## Reviewable source-only package

External copy:
`D:\THESIS-BACKUPS\cvl-bp1-preparation-oByPHd\prepared-source`

| Item | SHA-256 |
|---|---|
| New complete source manifest | `ddcb207aa016b8c3306ba8b0fd53cd9008e972f53b38e4801f8e45979914e0b6` |
| Protocol | `284b420a0b0bb183689ac217346cc9a1444ba814fc0d3c8cf064952e80ec0f9c` |
| Data-role manifest | `ae77734061becc33ac37d9f094c918c1c77a844da7d3d12af15471a3d2eb107f` |
| Inherited source manifest | `521a0e6627c570ffa30d12adc63f92cf9ba76dee2c9bc4c59d24f4982f4f45f2` |

The package has an explicit `researcher_approved: false` template. Its source
identity, protocol and role manifest can be reviewed before any later launch.
This receipt and README are publication records, not members of the executable
source closure; adding this receipt does not change that closure's hash.
The source freeze is content-authenticated, not an assertion of OS write protection.

## Bounded proposal, not consumed resources

- New-outcome nominal maximum: 12,288 breadth +12,288 precision +8,192
  evaluation =32,768. Original C availability fixes its increment at11,344,
  giving a known-adjusted total ceiling of31,824 before new-source reductions.
- Six fixed ensembles /18 fits. Each BCE fit:1,800 updates; each relative fit:
  1,560. Same accepted training-only normalization for A/B/C.
- Proposed envelope: **86 A6000 allocation-hours; two allocation-hours at
  4CPUs/8GiB for CPU stages; 10GB new remote artifacts; 40GB external-SSD free
  space.** Actual future runtime/accounting is required; estimates are not measurements.
- No new labels, real evaluator training, planner/world-model/simulator calls,
  Slurm jobs, reserved closed-loop access or 1600–5999 payload access occurred.
- Next decision: approve or reject this exact executable development proposal.
  No automatic experiment, model promotion or closed-loop follow-up is enabled.
