# CVL-BP1 second scoped host recovery — 17 September 2026

The user answered **yes** to repairing the external-SSD check and resuming only
the **302 unsubmitted tasks**, reusing all **148 completed coordinates** without
changing scientific settings or budgets. This approval follows the preserved
[recovery stop](RECOVERY-STOP-20260916.md). It is not permission to rerun completed
work, retry a scientific failure, replace a case, or change an endpoint.

## Exact scope and preserved accounting

The scientific source remains
`70e3838c83561b8b24ea2f2c2f6875c2674d7c9b391e682c1023ba3641238975`.
All workers invoke its original `run_breadth_precision.sh` with the original
execution approval, source-role manifest, pinned runtime, models, candidates,
streams, label policy, optimizer updates and analyzer. The 148 completed tasks
are the unchanged grid prefix: breadth 0–139 and precision 0–7. The next task is
`breadth-140`, source 463, H75. No interrupted output slot needs replacement.

The remainder is 52 breadth +184 precision +64 evaluation GPU allocations,
one CPU fit and one CPU analysis. All 149 prior allocation attempts remain
recorded: 148 successes plus the original cancelled 301578 (63 seconds).
The initial charge is **32,004 GPU seconds**, zero CPU scientific-stage seconds.
Prior charges plus all remaining maximum reservations are **250,404 GPU seconds**
and **7,200 CPU allocation-wall seconds**. A completed study would still have
450 successful coordinates and 451 total attempts. No new replacement allowance
is created. All original aggregate/per-job resource caps remain in force.

## Narrow infrastructure repair

The failed operation was repeatedly invoking `powershell.exe` through WSL to
read the external volume label. The replacement is a **native Windows PowerShell
helper**, launched hidden. Every 20 seconds it checks D:'s `THESIS_SSD` label,
the exact approved volume UniqueId, NTFS filesystem and at least 40GB free space.
It writes an atomic, approval/source-bound local status record on that same SSD.
It has no network, scheduler, model or repository-write capability. Its claim is
exclusive; no automatic restart is supported. It stops on a recorded companion
halt, a volume/check/write failure, or its seven-day lifetime limit.

The WSL backup companion does **not** launch Windows executables. It requires
that native record to be at most 90 seconds old (future timestamps are rejected),
with the exact approval, source, volume identity, label and headroom. It also
independently verifies `/mnt/d` is mounted and checks actual WSL free space.
Missing/stale status, wrong volume, insufficient space, or native stop is fatal;
no laptop fallback or permissions change is introduced. This avoids the known
interop call path without treating a failed identity check as success.

The remote controller retains the existing source/approval-bound backup lease
(maximum age 150 seconds, bounded wait 180 seconds) before **every** submission.
An already-running bounded job can finish during loss of backup liveness, but
the next job cannot start. Recorded backup failure stops immediately. Existing
bounded read-only transient SSH probes are reused; writes, archive transfers,
submissions and jobs have no automatic retries. A new failure preserves evidence
and requires scoped direction. Ambiguous submission is never repeated.

## New records, old records and backups

The new host code is frozen separately, including the unchanged reused host
helpers. Neither prior frozen source is edited. New authorization and control
files live under remote `staging/cvl-bp1-recovery2-20260917`; local controls live
under `D:/THESIS-BACKUPS/cvl-bp1-recovery2-20260917/controls`. The original run root
is retained. New exclusive records are `RECOVERY2-CLAIM.json`,
`RECOVERY2-DISPATCH.jsonl`, `RECOVERY2-STOP.json` if needed, and a sealed
`recovery2-evidence-20260917/`. Original dispatch/recovery ledgers, stops, approval
receipts, partial-attempt preservation and both external archives remain intact.

Prelaunch authentication uses the accepted terminal accounting and both backup
requests/ACKs, checks the exact 148 completed identities through the existing
technical-only reader, and refuses any already-created later task or stage.
It carries the original cancellation charge forward. Native and WSL processes
must verify exact approved scope; the controller must run the sealed overlay.

Both existing archives are verified before the companion advertises readiness:
`failure-20260916.tar` covers the original 102 outputs, and
`recovery-stop-20260916.tar` covers the next 46 and recovery evidence. Subsequent
incremental stage backups exclude those already covered roots. Their union with
the later train/models/evaluation/terminal archives must cover the final study.
The accepted streaming archive writer and exact member/seal verifier are reused.
Archive wall time and bytes are recorded; no backup GPU allocation is introduced.
Training backup precedes fitting; all 18 models are frozen and backed up before
evaluation; evaluation backup precedes analysis; terminal backup precedes final
completion. All preserved/failed bytes and charges remain inside the old caps.

## Verification and research boundary

Focused synthetic tests exercise the exact 302-task remainder, 451-attempt final
accounting, unchanged scientific worker target, stale/wrong/missing native lease,
mount/free-space rejection, no interop or laptop fallback, exclusive claims,
failure without retry, backup loss between jobs, immutable old ledgers, and
training/model/evaluation/terminal backup ordering. Tests run without any real
scientific execution; stdlib host compatibility is also checked on Prometheus.
Native PowerShell syntax and the actual read-only volume check are verified.
The source snapshot, authorization hashes, test counts and launch processes will
be recorded separately after verification and publication.

The research information barrier is unchanged. Partial performance remains
unopened until the full fixed study and backups complete. Reserved CVL closed-loop
32, all 1600–5999 payloads, historical results, `stop_no_ranking_promise`, the
historical `original_relative` nominee, continuation baseline and E12 drafts are
preserved. No scientific follow-up or method change is authorized by this repair.
