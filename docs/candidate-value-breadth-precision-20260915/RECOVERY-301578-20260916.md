# Scoped infrastructure recovery: cancelled allocation 301578

The user's **YES** authorizes repair of backup monitoring and resumption of only
the unfinished CVL-BP1 grid, with one replacement attempt for cancelled
`301578` (`breadth-94`, source reference 607, H75). This is not a scientific retry
or an amendment to CVL-1, its endpoint, the BP1 protocol, or any historical result.

## Unchanged scientific execution

All workers continue using the original immutable source manifest
`70e3838c83561b8b24ea2f2c2f6875c2674d7c9b391e682c1023ba3641238975`, original
execution approval, container, source-role manifest, model/checkpoint bytes,
sampling, streams, labels, update budgets, weighting, and analysis. The recovery
is a separately sealed **host-infrastructure overlay**, not a replacement
scientific snapshot. It invokes the original `run_breadth_precision.sh`.

There are 102 authenticated completed coordinates to reuse: breadth 0–93 and
precision 0–7. The original ledger, stop record, terminal accounting and accepted
failure backup remain unchanged. Accepted technical scalar projection is used;
no labels, probabilities, banks or partial scientific results are decoded. The
projection adds the report's actual top-level `horizon` field to check the fixed
task identity; this changes no report or worker.

## Exact recovery and accounting

The completed 102 allocations consumed 21,729 GPU allocation seconds. Cancelled
301578 consumed 63 seconds, which remain charged: **21,792 seconds total** before
resumption. The remaining fixed grid contains **348 coordinates**: 282 training
collection jobs (including the one replacement), one fit, 64 evaluation jobs and
one analysis. This is 346 GPU and two CPU allocations. A fully completed recovery
will therefore have 450 successful coordinates and **451 allocation attempts**,
including the one preserved cancellation. No other replacement is authorized.

The remaining maximum reservations plus all prior actual charges are **267,792
GPU seconds (74h 23m 12s)** and 7,200 CPU allocation-wall seconds. The unchanged
aggregate caps are 309,600 GPU seconds, 7,200 CPU wall seconds, 10GB remote
artifacts, 500MB per allocation, 50MB source/control and 40GB external free space.
Accounting includes failed/cancelled allocations and preservation artifacts.
The accepted first-16 resource gate is retained, not rerun. No extra references,
labels, model fits, evaluation cases or scientific stages are added.

Before the replacement, only the exact cancelled `breadth-94` and
`tmp-breadth-94` directories are relocated under
`interrupted-attempt-301578/` in the same run. Their bytes are checked against the
already sealed, externally backed-up original-path copies. No bytes are deleted
or overwritten. A sealed relocation map records both paths; the original-path
failure archive remains intact. This clears the logical output slot for the
unchanged worker's exclusive-create behavior. Any mismatch stops recovery.

The run's original `DISPATCH.jsonl` and `DISPATCH-STOP.json` are never amended.
New events use `RECOVERY-DISPATCH.jsonl`, with a one-use recovery claim and exact
authorization/source hashes. A new failure produces `RECOVERY-STOP.json`; it does
not trigger an automatic retry. Ambiguous submission is never repeated.

## Backup liveness repair

The external companion reuses the existing D: **THESIS_SSD** destination and
authenticates the existing failure archive and receipt. No laptop fallback is
permitted. Every 30 seconds it probes only control-file existence and publishes
a source/approval-bound liveness record with current external free space.

Read-only SSH probes have at most three attempts, with a 60-second timeout and
5/15-second backoff. Only transient connection failures qualify. Permission,
authentication and host-key failures stop immediately. Control writes and archive
transfers are **not automatically retried**; uncertain/partial transfers are
preserved for explicit reconciliation.

Before each new submission, the controller requires a liveness record no older
than 150 seconds; it waits at most 180 seconds for freshness, then stops without
submitting. An already-running bounded worker may finish during a transient
backup interruption, but no next worker starts without fresh liveness. Thus a
backup outage cannot silently continue dispatching the grid. A recorded backup
stop blocks immediately. Stage backups retain the one-hour deadline, with no
GPU allocated during backup waits.

The failure archive already covers the 102 completed coordinates and original
failure evidence. Incremental train backup covers the newly completed training
coordinates plus recovery/relocation evidence; it does not rearchive those 102
outputs. Fit is permitted only after training coverage is backed up. All 18
models must pass the unchanged freeze check and be backed up before the first
evaluation job. Evaluation backup precedes analysis; terminal backup precedes
the final completion record. Final coverage is the union of these disjoint
stage receipts and the preserved failure archive.

## Verification and boundaries

Focused synthetic host tests exercise the exact remaining grid, cumulative caps,
exclusive relocation, metadata identity, lease checks, bounded connection
retry, no permission fallback, no write/transfer retry, and simulated complete
dispatch including model-freeze/backup ordering and failure stops. They perform
no model evaluation, optimization, physics, label collection or real Slurm work.
The original preparation and host tests remain applicable. Python 3.6 stdlib
compatibility must also pass on the actual login host before resumption.

The reserved CVL closed-loop references, all 1600–5999 payloads, historical
scientific decisions and the unrelated E12 drafts remain untouched. No new
study, closed loop, SAGE/GMM run, threshold selection or follow-up is authorized.
Partial performance stays sealed until the full fixed study and backups finish.

Launch-specific hashes, test results and process identities are recorded in a
separate recovery launch record after the implementation is committed and
verified on the remote branch.
