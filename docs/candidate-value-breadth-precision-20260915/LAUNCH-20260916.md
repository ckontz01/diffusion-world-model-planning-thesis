# CVL-BP1 approved launch — 16 September 2026

## Authorization and status

The researcher approved the bounded study prepared at
`9ea4c58abaa6c907ae539020126b3f427666eedd`. The subsequent explicit user instruction
“yes, fix it” authorized the dependency-only host repair documented in
[HOST-REPAIR-20260916.md](HOST-REPAIR-20260916.md). Repair commit
`3d71d4c1ca21009dbe59ef07944d4b521f56cc97` was pushed and its remote hash verified
before launch. The original package and zero-allocation pre-launch blocker remain
preserved; this is not a scientific retry.

The repaired serial controller started on Prometheus at **2026-09-16 10:30:00 UTC**
(PID **3489337**). It submitted **Slurm 301452**, `breadth-0`, reference **333**, H75,
at **10:30:16 UTC**. The first scheduler check reported `RUNNING`, 55 allocated
seconds, 4 CPUs, 24GiB and one GPU. A running allocation's `0:0` is not a terminal
success claim. Collection, the first-16 technical gate, fitting and evaluation
were **not complete** when this launch record was written. No partial scientific
outcome was inspected.

Before publication, the dispatch ledger recorded **301452 COMPLETED, exit0:0,
175 allocation seconds** and submission of **301453**, `breadth-1`, the same
reference333 at H150. The controller authenticated the first job's sealed
technical metadata before advancing. This is one completed allocation, not
passage of the 16-job gate or a scientific result.

## Bound identities and locations

| Item | SHA-256 |
|---|---|
| Frozen source manifest | `70e3838c83561b8b24ea2f2c2f6875c2674d7c9b391e682c1023ba3641238975` |
| Unchanged protocol | `284b420a0b0bb183689ac217346cc9a1444ba814fc0d3c8cf064952e80ec0f9c` |
| Unchanged resource plan | `577ac440e26fb4b18d456af3581b2b0d9ddf254d0166295944dcfc0743eb938d` |
| Unchanged role manifest | `ae77734061becc33ac37d9f094c918c1c77a844da7d3d12af15471a3d2eb107f` |
| Researcher approval attachment | `5fd32eaa897f8a468ac7516f091f5eaaed16e41df9e88afac826c25ff53927bc` |
| Bound execution approval | `e7f6d1e67b9ccc0babfb7b613806dc3826fc9d9dc0225c93fab7b54c424d9bc5` |

- Snapshot: `/lustreFS/data/superworld/ckontzias/thesis/snapshots/candidate-value-breadth-precision-20260915-70e3838c83561b8b`.
- Run: `/lustreFS/data/superworld/ckontzias/thesis/experiments/candidate-value-breadth-precision-20260915/run-70e3838c83561b8b`.
- Approval, authentication and controller receipts:
  `/lustreFS/data/superworld/ckontzias/thesis/staging/candidate-value-breadth-precision-20260915-70e3838c83561b8b`.
- External source copy:
  `D:\THESIS-BACKUPS\cvl-bp1-preparation-oByPHd\host-repair-source-20260916`.
- External launch controls:
  `D:\THESIS-BACKUPS\cvl-bp1-preparation-oByPHd\launch-control-70e3838c`.
- External artifact destination:
  `D:\THESIS-BACKUPS\candidate-value-breadth-precision-20260915\run-70e3838c83561b8b`.

The source approval template remains false and unchanged. Execution uses the
separate hash-bound `EXECUTION-APPROVAL.json`, not a modification of the snapshot.

## Pre-launch checks and backup readiness

All 81 source-manifest members verified. The source plus manifest occupies
872,747 bytes. The existing 26 synthetic tests and four new isolated host-boundary
tests passed; all four host tests also passed on Prometheus's actual Python3.6.8
without NumPy/PyTorch. The scientific learning function bodies and protocol,
collection, role and runtime-wrapper bytes were unchanged by the repair.

Read-only authentication verified the pinned runtime files, environment/code
trees, old capsule and accepted normalizer. It took 79.434 wall seconds and
recorded zero research allocations, new reference payload reads, model/physics
execution or runtime dependency changes. The controller subsequently repeated
its own required authentication before submitting the first job.

The backup companion started in WSL `Thesis-Ubuntu` (PID **17325**). It verified
`/mnt/d` and the Windows volume label `THESIS_SSD`, then recorded
**378,183,561,216 free bytes**, exceeding the unchanged 40GB reserve. The companion
was live before dispatcher launch. It has no job-submission or automatic-restart
authority. The external drive must remain connected and WSL running for backup
gates to complete; a missing acknowledgment makes the controller stop, not skip
the gate.

The four remote control files were copied to the external launch-control directory
and their SHA-256 values independently matched:

| File | SHA-256 |
|---|---|
| `PRELAUNCH-AUTHENTICATION.json` | `19d3ba9a5438446bdb0900151f52c73fccf9251442bfdd6abf77485d4d56e5fc` |
| `BACKUP-READY.json` | `cdb739d2586dd0a04f4e77d3d2b2eaaa41bd55837cdf82a3826a3e4047434956` |
| `CONTROLLER-PROCESS.json` | `dce3f7e2381aae7db2436715484b363ef83a9e81eb949b10178ca5dcbabf7dee` |
| `EXECUTION-APPROVAL.json` | `e7f6d1e67b9ccc0babfb7b613806dc3826fc9d9dc0225c93fab7b54c424d9bc5` |

This is verified **source/control backup readiness**, not a claim that future
research outputs have already been backed up. Those receipts are required at the
train, model, evaluation and terminal gates.

## Unchanged execution envelope

The fixed grid is 384 B/C GPU jobs (600s each), 64 evaluation GPU jobs (1200s each),
one CPU fit (5400s) and one CPU analysis (1800s): **448 GPU plus two CPU allocations**.
Only one study A6000 allocation runs at a time. GPU jobs use 4 CPUs/24GiB; CPU jobs
use 4 CPUs/8GiB. The aggregate limits remain 309,600 GPU allocation seconds,
7,200 CPU allocation wall seconds, 10GB remote artifacts, 500MB per job and 50MB
source/control reservation. Failures count. The spare 40 GPU minutes do not
authorize retries or additional jobs.

The first four breadth and four precision references, both horizons, are the
first **16 included jobs**, not extra pilots. Their frozen memory/time/storage
projections determine technical feasibility without inspecting scientific
promise. Passing allows the unchanged remaining collection, backup, all 18 fixed
fits, authenticated model freeze and backup, common evaluation, analysis and
terminal backup. No additional research-approval checkpoint is introduced.

The nominal maximum is 32,768 new binary outcomes. The known original-bank
unavailability reduces C's new outcomes from 12,288 to 11,344, yielding a known
ceiling of 31,824 before further unavailable anchors. These are allocation
counts, not observed results or independent-source counts. Deterministic
final-budget repetitions are not additional independent tail evidence.

On a technical/controller failure: preserve outputs, reconcile any active
allocation and charges, back up terminal partial artifacts, and request a scoped
recovery decision. No automatic retry, replacement, permission/dependency change
or expansion is authorized. Scientific underperformance remains a result.

## Monitoring and research boundaries

The thread heartbeat **`monitor-cvl-bp1`** was created for 20-minute checks. It
reads technical dispatch/scheduler/backup state, reports only meaningful changes,
and never owns dispatch or restarts the controller. Until the full fixed run and
its backups finish, no partial labels, predictions or success metrics are read.
Exact failed technical logs may be inspected only after identifying the failure.

The reserved CVL closed-loop32 and all1600–5999 payloads remain out of scope.
There is no closed-loop study, SAGE/GMM benchmark, confirmation restart, new
proposer training or automatic follow-up. Historical artifacts/decisions,
`stop_no_ranking_promise`, the recorded `original_relative` nominee and unrelated
E12 drafts are unchanged. Continuation remains the working baseline.

Completion requires all 450 coordinates, resource accounting and verified backup
coverage, followed by the approved all-configurations/all-references report. This
launch record neither promotes a model nor claims study completion.
