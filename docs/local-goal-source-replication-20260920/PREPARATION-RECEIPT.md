# LGP-RB2 executable preparation receipt — 20 September 2026

**Later narrow review:** the preserved v4 package below is not launchable because
duplicated indented evidence can exceed its unchanged job cap. The
[v6 lossless storage correction receipt](../local-goal-source-replication-publication-20260920/STORAGE-CORRECTION-RECEIPT.md)
records the new tested package, exact bound and 38 distinct tests. Original v4
hashes and receipts below remain historical; research execution is still disabled.

Status: **prepared and synthetically tested; research execution disabled**.
The user's advance delegation and the selected GPT-6 Pro preparation instruction
are recorded in [REASONING-INSTRUCTION.md](REASONING-INSTRUCTION.md). The same
conversation must review this package and issue an explicit execution instruction.
No research checkpoint forward, physics, optimizer, Slurm submission or new label
generation occurred. Historical LGP1/RB1 decisions and artifacts are unchanged.

## Source and data contract

Exactly 512 references were selected by the prescribed identifier-only rule from
1,280 eligible exposed references, after excluding a union of 320 registered
sources. The complete ordered list, overlapping exclusions, original metadata
hashes and identity-reader certificate are in [DATA-ROLES.json](DATA-ROLES.json).
Allocation digest: `865418138af0f3858f3a677008f717a724bf7ac2bf72881d909c9d0b79bcd000`.
No selected reference NPZ was opened or hashed. These are method-role-disjoint
development sources, not untouched confirmation or universally source-unseen data.

Exactly 1,536 eight-episode GPU jobs plus one CPU analysis: 12,288 new episodes,
four arms GMM5/diffusion5/GMM30/diffusion30, three original seeds and two horizons.
The first four main jobs include the 32-episode technical/resource tranche, with
no extra query, pilot or repeat. No models are fitted. Six final models and the
eight reused scientific source components are pinned by [REUSE.json](REUSE.json).
See [PROTOCOL.md](PROTOCOL.md) sections 3–7 for lifecycle, seeds, endpoint,
balanced order, shared-source analysis and fail-stop dispatch.

## Immutable tested export

Designated SSD export:
`D:/THESIS-BACKUPS/local-goal-source-replication-20260920/preparation-v4`.
Archive: sibling `preparation-v4.tar`, 3,768,320 bytes, 108 file members;
whole archive and every member independently verified during packaging.

| Binding | SHA-256 |
|---|---|
| Source manifest | `10b147c3e69009fcddee7b532fb960195defaab3b4e4731a9ddb3ab943666c41` |
| Source archive | `94ff1df11006dcec48669581ed9b3e7929475d93a0f111f94b4f134743f10900` |
| INPUTS.json | `dad31b016748698595d980cd962d632725f140a1aa1091dc7add58ef562dcca8` |
| DATA-ROLES.json | `daa652174a7bf2f459190d0ddcea8524218a392fc1644b345a4ce94f8fc38536` |
| GRID.json | `6f911efe1c39848a91cd7d30c7436321315c1a16ce8307bd5402dd0e539618bc` |
| PROTOCOL.md | `f508ec6389067d6a3538914ca39a9a4c08dc9c3d9dc889e07c1c283c6b2952cf` |

Export v4 layers new code on the exact accepted RB1 export; inherited files may
not be replaced. It contains an `execution_authorized: false` approval template.
This receipt and TEST-RESULTS.json are publication evidence outside that frozen
manifest, avoiding a circular receipt hash. Earlier preparation exports are
preserved, not overwritten. A later execution approval must bind the actual
manifest, protocol, input, roles, reuse and grid hashes and all fixed caps.

Published outside the immutable source closure:
[exact source manifest](../local-goal-source-replication-publication-20260920/SOURCE-MANIFEST.sha256),
[disabled template](../local-goal-source-replication-publication-20260920/APPROVAL-TEMPLATE.json)
and [archive/member receipt](../local-goal-source-replication-publication-20260920/PACKAGE-RECEIPT.json).

## Tests actually run

**35 tests passed** from v4 in pinned Python 3.11.10 / Torch 2.5.1+cu121 /
NumPy 2.2.6, CPU only, one thread, no GPU passthrough or CUDA initialization.
33.931453 seconds wall, 33.184738 process CPU seconds, 523,309,056-byte peak RSS.
The [complete test receipt](TEST-RESULTS.json) names all tests and binds v4.

Coverage includes exact allocation/grid/order; immutable approval rejection;
host-only imports and disabled entrypoints; charge-before-stop and ambiguous
dispatch; full remaining reservations; complete source-weighted analysis;
eight fresh episodes in both arm orders using artificial model/vector-pool
inputs with actual samplers/CEM/policy; action/round equality with the prior
two-horizon driver; bank identity and rejection; native endpoint rejections,
first-chunk and final-step success; pinned decoder/criterion and lifecycle.
These are artificial fixtures, not research inference or simulator executions.

The exported Bash wrapper also passed `bash -n` on Prometheus, and the controller,
contract, analysis and launcher imported successfully with host Python 3.9 `-B -S`
(no site packages). The existing WSL view of D: returned `No such device` for a
local syntax-check path; the exact already-uploaded export was checked remotely
instead. Native Windows SSD access remained healthy. The unrelated WSL filesystem
was not repaired or changed.

Final synthetic staging on Prometheus:
`/lustreFS/data/superworld/ckontzias/thesis/staging/lgprb2-synthetic-10b147c3e69009fc`.
This is a preparation-only source/test directory, not a scientific run/launch.

A preparation-only v2 transport attempt exposed Windows-to-POSIX path
serialization before its tests started. Its source-only staging is preserved;
Apptainer did not start and no scientific work was performed. The path builder
and newly generated input-path serialization were corrected, with a focused
regression. v3 then passed 34 tests; v4 adds the exact-approval rejection test
and full future-archive reservation and passes all 35. No frozen historical
source, dependency, permission or separate WSL filesystem was modified.

## Cost and launch proposal, not expenditure authorization

[RESOURCE-PLAN.md](RESOURCE-PLAN.md) uses actual prior allocation charges and
full-budget planning adjustment, retaining four old setup charges rather than
assuming a batching speedup. Scenarios are 68.19 GPU-hours, or 85.23 hours with
25% overhead. Proposed hard envelope: 1,536 × 300 seconds = 460,800 GPU seconds
(128 hours), serial A6000, 4 CPUs/24 GiB; one 4-CPU/8-GiB, 7,200-second CPU analysis.
No automatic retry/resume/requeue, extra allocation, case substitution or cap
change. A failed first-four tranche stops, preserves and reconciles charges.

8 GB new-worker cap; 5 MB/main job plus 200 MB analysis reserve 7.88 GB.
200 MB source/control/log cap; 24 GB inclusive history/live work/final archive.
Full work plus archive and historical occupancy reserve about 19.87 GB with tar
overhead allowance. Endpoint evidence remains compact; no raw banks or videos.
THESIS_SSD identity `0a2f1ba9-0000-0000-0000-100000000000` and at least 40 GB free
were verified for preparation; destination is the study directory above.

Real two-model packaging, whole-worker memory, new-source behavior and throughput
remain unverified until the included authorized tranche. A 1.5× slowdown approaches
the 240-second worker preservation boundary and can cause a technical stop.
All 512 effects are reported, no old32 pooling or model promotion. The primary
is diffusion5 minus GMM5; secondary interactions/absolute success/cost remain
descriptive development evidence. Better timing is not noninferiority.

## Executable entrypoints (still disabled)

- `lgprb2_package.py`: exclusive LF export, inherited source lock and archive verification.
- `lgprb2_test_receipt.py`: focused artificial tests from the export.
- `lgprb2_launch.py` / `lgprb2_dispatch.py`: exact approval, exclusive controller,
  serial full-grid reservations, included tranche and no retries.
- `run_lgprb2.sh` / `lgprb2_worker.py` / `lgprb2_evaluate.py`: pinned runtime,
  fresh multi-arm episodes, byte/time guards and evidence.
- `lgprb2_analysis.py`: full grid, endpoints and source-cluster contrasts.
- `lgprb2_preserve.py`: sealed final new archive plus whole historical union on SSD.

No execution namespace or approval has been activated. Return this immutable
preparation to the selected reasoning conversation for source/grid/resource review.
