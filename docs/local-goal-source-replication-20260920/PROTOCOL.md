# LGP-RB2 — frozen source-disjoint development replication

## 1. Status and question

Preparation under the user's advance delegation to the selected GPT-6 Pro chat.
The complete reply (worked for 21m15s) directed preparation, not execution.
Explicit execution instruction in that same conversation is required after
review of this source/grid/resource package. APPROVAL-TEMPLATE.json is false.
There are no launch, research forward, optimizer or physics calls in preparation.

Question: does diffusion improve native success over GMM at five total scored
populations on different development sources? Does the family effect change
between five and thirty? No new algorithm or trained component is introduced.
LGP1/RB1/CVL/SI1 decisions remain unchanged. Retain continuation; promote neither
proposer. No more tuning on the old32 or prospective512 cohort.

## 2. Source allocation and information boundary

DATA-ROLES.json contains the exact ordered512, exclusion lists/counts, canonical
metadata hashes and identity certificate. From exposed0–1599, exclude320 distinct
registered sources;1280 eligible,512 allocated,768 remain. Overlapping role counts
must not be added. CVL96train/32validation/32reserved plus historical32, BP1additional
96train/32evaluation make320. SI1 reuses the192training union; LGP1/RB1 reuse the
bottleneck/single-anchor32 and two technical members. Objective/capacity and learning
diagnoses reuse CVL roles, not additional source assignments. Expert P1 episode
IDs are another namespace: do not subtract them as independent-reference integers.
Frozen components' historical exposure remains unchanged; source-unseen is not claimed.

Order eligible IDs by SHA256 UTF8 `lgp-rb2-source-v1|<decimal-reference-id>`, then
integer ID on digest ties; select first512. Allocation digest uses SHA256 of
canonical compact sorted-key JSON of that ordered list. All1600 identity records
were checked for distinct namespace/attempt keys using the accepted lexical reader.
Only index/file/SHA256/environment_seed/namespace/attempt were decoded. No outcomes
or reference NPZ was opened or hashed during allocation. Every selected NPZ's
recorded digest is pinned in INPUTS.json and checked inside future charged jobs.

The population was exposed in the original benchmark: this is recent-method-role-
disjoint development, not untouched confirmation. Reserved CVL closed-loop, BP1
evaluation,1600–5999 payloads and all earlier protected artifact boundaries remain
closed. No historical32 result enters the new primary estimate. No replacement
source, favorable seed, difficulty selection or retrospective cohort modification.

## 3. Exact scientific settings and inputs

Four arms: GMM5, diffusion5, GMM30, diffusion30. Six final LGP1 models, training seed
blocks8301/8302/8303, H75/H150,15primitive-action chunks, physical budgets150/300.
All old models/normalization/decoder/initializer/local-target generator/LeWM,
FP32 CEM,300candidates,30elites, stable ties, support projection and projected
final-elite-mean return are byte-pinned and unchanged. Populations are total scored
populations, including the initial proposal. No best-candidate return or new
threshold/CFG/sampler/score mixture. INPUTS.json replaces only the allowlisted
reference file entries; the historical input lock remains preserved separately.

Proposal seed derivation remains `stream_seed((reference,horizon),absolute_stage,
'proposal',training_seed)`. Budget and arm order do not enter it. Refinement uses
the distinct historical refinement stream. Each trajectory uses its own actual
state/history after actions diverge; no shared future actions/banks are imposed.

## 4. Grid, packaging and lifecycle

512sources ×3seed blocks ×4arms ×2horizons =12,288 new episodes in1536 GPU jobs,
one source/seed per job; all eight cells included. One final CPU job; no GPU pilot,
training, old episode reuse or extra compatibility allocations. GRID.json is
executable and immutable. Schedule source-allocation order, then ascending seed.
First four jobs:74/8301,74/8302,74/8303,1592/8301. Their32episodes are included main
data, not a discardable pilot; no extra forwards or episode repetitions.

Balanced order: rank all1536(source,seed) pairs by SHA256
`lgp-rb2-order-v1|<source>|<seed>`, integer pair ties; assign rank modulo8 as a cyclic
rotation of [(gmm5,H75),(gmm5,H150),(diffusion5,H75),(diffusion5,H150),
(gmm30,H75),(gmm30,H150),(diffusion30,H75),(diffusion30,H150)]. Each cell occupies
each position192times globally. Scheduling order and episode order are separate.

Load one frozen shared backend and both seed-specific frozen proposers once per
job. Reuse only their read-only weights/statistics. Construct a NEW World,
FreshEpisode, Policy, solver, buffers, observation history and explicitly seeded
proposal/refinement generators for EVERY cell. Close each World even on failure;
finish every policy. Model tensor hashes before/after each cell must match.
No legacy dataset evaluator. Artificial tests compare the actual policy/CEM and
old two-horizon driver against new packaging, both orders, both families/budgets.

Each job checks first-stage context, raw/projected initial-bank hashes across
budgets within each family/horizon. It does not compare different trajectories
at later stages. Technical checks use included executions, not additional queries.

## 5. Endpoint, cost and evidence

Primary endpoint: unchanged native post-action success under the fixed physical
budget. Combined agent/block position norm<20 AND reviewed angle difference<pi/9
at the same step. t0 excluded. Save initial/goal identities, delivered FP32 actions,
post-action raw states, terminal/truncation flags and absolute schedule. Require
full budget absent legitimate termination, no post-terminal work. Reuse the exact
independent lgp1_endpoint verifier including angle domains and tolerances. Each
cell has its own directory to prevent horizon-file collisions across arms.

All four arms have common timing instrumentation. Complete-episode wall starts
before World construction and ends after closing; it includes initialization,
planning, physical delivery, evidence save/verification/close. Shared model/backend
setup is separately measured once per job, not falsely assigned to an arm. Save
World creation/reset/close, evidence verification, proposal/refinement/context/
total-planning/scoring/hash totals, stages/actions/calls, job wall/processCPU,
Python RSS and Torch allocated GPU peak. Scoring is inside refinement; hash timers
overlap planning/refinement and cannot be simply added or blindly subtracted.
Complete allocation includes wrapper/authentication/sealing; queue wait excluded.
Report memory scopes honestly (not whole process-tree or total GPU reserved memory).
Success/GPU-hour is not a replacement endpoint.

## 6. Analysis frozen before new outcome access

Single primary contrast delta5 = diffusion5 − GMM5. Secondary: delta30, interaction
delta5−delta30, diffusion5−diffusion30, diffusion5−GMM30; absolute rates for all four.
Equal source weight; equal two-horizon/three-seed weights within source. Report
all512source effects,24horizon/seed/arm strata, all12,288 identities and outcomes.
Common whole-source percentile bootstrap,10,000 resamples,seed20260920,95%intervals.
No old32 pooling, seed pseudoreplication, selected budgets or noninferiority margin.
Primary interval concerns this new development cohort; secondary intervals are
descriptive, not multiplicity-adjusted discoveries. Significance in one arm/budget
and not another is not evidence of interaction. No automatic efficacy gate or
promotion; return the complete result to the reasoning conversation for interpretation.

## 7. Dispatch, stops, preservation

One A6000 allocation at a time,4CPU24GiB,300s/job. Worker240s soft boundary leaves
60s preservation margin. First four successful sealed main jobs must pass all
technical/endpoint/model/resource checks before fifth submission. Only technical
metadata/seals/costs may be inspected then, not success or partial effects.
The 300s/job resource binding remains fixed; no tranche-based budget tuning.

Full remaining allocation reservations, byte reservations and exclusive namespaces
are enforced before submission. No prior attempt may exist. sbatch ambiguity is
recorded and stops; terminal charges precede failure handling. No retry/requeue,
resumption, extra allocation/case/configuration/cap changes. A technical stop
preserves all partial/successful artifacts and goes to the delegated reasoning
conversation for a concrete recovery decision; poor outcomes are not technical.

Cluster-resident detached controller, no laptop/SSD liveness gate while computing.
One4CPU8GiB7200s CPU analysis after all1536worker seals. Information barrier until
all1537jobs, independent endpoints/source/model/grid/order/resource authentication
and byte/member-verified final SSD union backup are complete. New archive covers
source/approval/models-by-pinned-historical-union/new endpoints/reports/accounting/
controller/logs. Reverify complete historical LGP1 and RB1 archives, preserving
all old provenance. Only designatedTHESIS_SSD,>=40GBfree, no fallback/overwrite.
Any failed transfer remains preserved; no automatic repeat.

## 8. Interpretation and missing runtime evidence

RB2 tests transfer of a development-selected operating point; it is not a new
method, untouched confirmation, SAGE comparison or broad-task evidence. A positive
primary result need not establish budget dependence. Any negative result stays
negative without another round-count search. Real multi-arm loading, peak memory,
throughput and new-source endpoint execution remain untested until authorized
included tranche. Synthetic success is not a performance promise.
