# Required SSD / configured transport unavailable

The designated THESIS_SSD was absent from native Windows `Get-Volume` checks on24September2026. Drive D: could not be found. The configured read-only metadata query through Thesis-Ubuntu and SSH alias `prometheus` therefore did not reach the cluster.

An independent connection diagnostic returned:

```
Failed to attach disk 'D:\WSL\Thesis-Ubuntu\ext4.vhdx' to WSL2:
The system cannot find the path specified.
Error code: Wsl/Service/CreateInstance/MountDisk/HCS/ERROR_PATH_NOT_FOUND
```

This is not evidence of a cluster failure. No scheduler, research worker, GPU, simulator, LeWM forward or evaluation payload was accessed. No disk/WSL repair, permission change, credential extraction, alternate SSH configuration or substitute backup destination was attempted.

The attempted metadata check and subsequent local-only work are retained in `ATTEMPTS.jsonl`. `RECONCILIATION.json` explicitly distinguishes local historical metadata checks from the unavailable new live check. The fixed512 IDs have not been replaced or allocated.

Resume preparation when the exact designated SSD is reconnected: perform the metadata-only live reconciliation, inspect any conflict without changing IDs, then source-freeze the tested package and verify its new small SSD copy. A conflict remains a blocker. Even after those steps, research execution remains disabled until a separate explicit execution instruction. The closed historical monitors and studies remain untouched.
