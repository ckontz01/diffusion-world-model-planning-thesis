# ACV mechanism replication — preparation package

**Preparation only. No research execution authorized or launched.** ACV0 remains preserved at `5d8666754ad8aa0507730f86cf3ee6caa32c105a`; the old monitor stays paused. This package is separate from every executed source closure.

Start with [the saved-data mechanism report](SAVED-DATA-MECHANISM.md) and [the exact proposed protocol](PROTOCOL.md).

## Contents

- `committed_feedback.py`: the one new controller and supplied-runtime interface; existing controllers imported unchanged.
- `test_mechanism.py`: artificial policy/terminal/tie/action-locality/accounting/executor-interface tests.
- `saved_report.py`, `decomposition.py`: the one authenticated CPU-only saved-data computation; exclusive output creation.
- `SAVED-DATA-MECHANISM.json`: all32 actual choice/outcome pairings, all16 validation same-candidate banks, every predicted prefix component, member authentication and resources. No old-final outcomes assigned to the new control.
- `ELIGIBILITY.json`: reconciled metadata and all512 exact proposed identities; no reference payload opened or source allocated.
- `PROTOCOL.json`, `GRID.json`, `SENSITIVITY.json`:8192 actual episodes/8197 tasks, three fixed model pairs, finite cost and preservation reservations, execution disabled.
- `protocol.py`: executable protocol validator and task-spec emitter; `--execute` refuses.
- `planned_analysis.py`, `test_protocol.py`: source-paired estimator, seed-separated summaries, fixed seven-contrast multiplicity and sensitivity checks.
- `AUTHORIZATION.md`, `ATTEMPTS.jsonl`: scope and every bounded scripted attempt; original-code hashes in `base.py`.

## Safe inspection commands

From this directory with the existing Python/NumPy environment:

```text
python -B protocol.py --check
python -B protocol.py --task analysis
```

Do not rerun `saved_report.py`, `render_report.py`, `prepare_proposal.py`, or `protocol.py --freeze`: their exclusive result files already exist. No additional reproduction or backup cycle is necessary for inspection. No command here submits Slurm jobs, launches training, loads Le-WM or allocates sources. The supplied-runtime adapter is inert unless a caller provides a separately authorized executor.

## What this does not claim

Action changes are not automatically successful information gathering. Saved validation branch replay is not a new final evaluation. Three proposed fixed pairs are not three independent source cohorts or evidence already obtained. The8197-task proposal is not a staged/authorized cluster controller; future authorization must freeze its new runtime enforcement and output namespace before any launch. No favorable seed selection, safety, novelty, model promotion, automatic next stage or AV1 launch is implied.
