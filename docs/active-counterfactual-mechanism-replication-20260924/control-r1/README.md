# ACVM1 recovery R1: container bootstrap and finite continuation

The user's new direct instruction is preserved in `INSTRUCTION.txt`: “okay fix all technical issues and lunch it. I authorize you”. It supersedes the prior requirement for a separate recovery direction for diagnosed technical faults. This R1 implements exactly one replacement of failed CPU allocation304589, followed by the8196 never-submitted tasks. It is not scientific redesign, a new source allocation, resource expansion, unlimited retry authority or a resubmission of successful work.

## Diagnosed fault and minimal runtime correction

The original CPU batch script invoked host `/usr/bin/python3.9` on gpu02, where it was absent. The replacement bootstrap uses the already accepted ACV0 container-first pattern: authenticate the same image, invoke existing Apptainer with the same readonly science binding and writable run binding, and run Python3.11 from the same pinned worker runtime **inside** that image. No host Python installation, node permission change or dependency repair is performed. CPU visibility is empty; GPU visibility, exact gpu09/gpu09.cluster checks, all thread settings and container identity remain fixed.

The bounded watchdog now runs inside the container and invokes `worker_r1.py`. It retains the4096-byte per-worker-stream caps and explicit overflow/failure recording,290s GPU supervisor deadline and original CPU deadline. Slurm's unchanged300s/7200s limits also include startup. Container/bootstrap Slurm logs are retained and checked at acceptance against the original4096-byte cap; the inner watchdog does not claim to intercept pre-container messages. The original controller runtime remains host Python3.9, authenticated on the login host. Direct compute-node SSH diagnostics returned255 with no details; no permissions were changed or alternative direct-access route attempted. The accepted scheduled container route is retained.

`worker_r1.py` is a mechanical adapter of the frozen worker. Its evaluation and array-digest functions are byte/AST-identical. Only separate recovery authority, `submissions-r1` lookup and recovery approval metadata are added. All fitting/model/analysis/policy/physics components load unchanged from the original authenticated snapshot. No old scientific module or executed source is patched. The new controller validates the original retained reused-input seal rather than copying/refitting it again.

## Identity, attempts and old evidence

Original run/source/worker approval are fixed by `r1.py`; original scientific manifest is9f91156fd11a5d54… and worker approval22cf11af0742c4a1…. `BASELINE.json` authenticates all99 original source/control/run files, including the original STOP, failed submission, failed logs and reused checkpoints/data. It records one terminal failed allocation and zero successes, live jobs or ambiguous submissions. The original submission and `.slurm.*` files are never overwritten. The replacement uses `submissions-r1/fit-pair1-joint.json` and `.r1.slurm.*`; subsequent never-submitted tasks keep their canonical Slurm log names.

Finite R1 maximum:8197 new allocations,8198 cumulative attempts including304589,8197 successful logical tasks,8192 unique evaluation episodes. Prior elapsed charges are0 GPU and0 CPU allocation-wall seconds at scheduler whole-second resolution, not a claim of literally zero machine work. Full-future reservations remain2,457,600 GPU seconds and36,000 CPU-stage seconds. One GPU serially, original task limits,1GB non-evaluation group,18GB live,18.5GB archive and80GB inclusive remain unchanged. The attempt increase is explicitly authorized by the new direct instruction following the failure report; historical8197-attempt authorization is preserved rather than rewritten.

The R1 controller is one-shot. It rejects pre-existing task outputs, new STOPs, unknown failures, ambiguous submissions and duplicate/restarted campaigns. It writes a separately dated `HISTORICAL-RESOLUTION.json` binding the exact old STOP and new authority. Any new failure is preserved and diagnosed before another finite recovery package; R1 itself never retries or restarts automatically. No scientific result or poor throughput is repair authority.

## Acceptance, information barrier and preservation

All six models freeze before evaluation; first16 included evaluations pass the original technical gate before source2; final analysis retains the full-array prefix coupling, fixed seven contrasts and all512 sources. No partial outcome is read for recovery or continuation. Existing caps, seeds,192 updates,64 fitting/16 reporting-validation roles,8192 episodes and original early-replan reuse remain unchanged.

`finalize.py` independently reconciles all8198 allocations, compares304589 with its exact historical record, accepts8197 unique successful suppliers and binds every worker's recovery approval. `preserve_entry.py archive` creates one archive with five flat roots: original scientific source, recovery source, original control, recovery control and run. All hidden files, old/new logs and failures count. Native `transport.py backup` uses the existing one-attempt whole-file/every-member verification on the designated SSD. No historical-study archive is recreated. Only the new small recovery package is copied before launch.

Preparation validation is artificial only: at most3600 cumulative wall seconds,4CPU/8GiB,250MB. It tests actual8197-task control flow with mocked scheduler,8198-attempt final reconciliation, failed/ambiguous accounting, restart rejection, unchanged scientific functions, invocation/resources, watchdog caps/deadline and all archive roots/member rejection. No efficacy sweep, research fit, simulator, model inference or extra Slurm allocation is used. All failed preparation attempts, if any, remain recorded.

Remaining live uncertainties include availability of the existing container executable/runtime on scheduled nodes, real fitting/evaluation behavior, hardware association, resource tails, scheduler service latency and eventual large transfer. Artificial validation is not a claim that these have already succeeded.

Preparation history is retained: the first run caught helper import-name shadowing; the second caught an incomplete technical-record mock in the full-grid finalization fixture. Review also replaced a helper alias that collided with existing local variables; direct artificial worker-main and actual five-root archive-builder tests were added for that path. `derive.py` and `SOURCE-DIFF.patch` retain the initial adapter-generation record. `FINAL-SOURCE-DIFF.patch` is the authoritative final mechanical diff; none of the generation/audit helpers is a campaign entry point. No cluster task was used for these corrections.
