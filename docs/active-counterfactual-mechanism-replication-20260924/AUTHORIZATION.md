# ACV mechanism replication: preparation only

Direct user instruction, 24 September 2026: prepare ACV mechanism replication; do not launch new research execution. Preserve completed ACV0 at `5d8666754ad8aa0507730f86cf3ee6caa32c105a`.

Authorized deliverables: one CPU-only saved-data mechanism report (all32 actual choices/outcomes, prefix-value decomposition,16 validation banks with same-candidate comparisons); one committed-prefix/feedback-suffix control alongside unchanged static/no_update/active; tests; one exact source-disjoint512-reference protocol proposal with three fixed joint/ordinary seed pairs, unchanged64 fitting sources/192 updates, source-level inference, sensitivity/multiplicity/stopping/cost/preservation plans.

This is not execution approval. No Le-WM forward, physics, new fitting, collection, new reference payload reads, production integration, GPU or Slurm launch. Existing models may be evaluated by their small NumPy predictors on authenticated saved features. The only remote preparation operation is the existing identity-only registry projection needed to reconcile eligibility before proposing512 sources. Proposed identities are not reserved or allocated. Protected/reserved roles stay closed. Old32 outcomes are not in the new primary estimate. No monitor restart, automatic next stage or duplicate reasoning handoff.

Self-imposed preparation ceiling: one cumulative hour of local scripted tests/analysis, at most four CPU threads,8GiB process memory,250MB new artifacts. Standard existing Python/NumPy only; no dependencies changed. Saved report is exclusive-create and run once. Technical tests use artificial interfaces, not physics. Selected existing archive members are authenticated against the already-verified request; no new historical archive, transfer or backup cycle is requested or performed.

The future protocol's larger compute/storage envelope is a proposal, not an expansion of the current authorization. Historical ACV0, AV0, LGP decisions and all E12 drafts remain unchanged.
