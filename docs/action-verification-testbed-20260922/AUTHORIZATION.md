# AV0 preparation authorization

User instruction on 22 September 2026: "now lets move forward with something different: Do what it says in AV0". The attached AV0 request authorizes implementation, artificial CPU testing, documentation, commits, pushes and a small verified THESIS_SSD backup. It does not authorize a research launch. This is the current direct user request, not authority inferred from an older heartbeat or a paper.

Request attachment: `C:/Users/Chris/.codex/attachments/c017cffb-6296-4c5a-9646-41ecea75f731/Pasted text.txt`. Supporting audit files are guidance; their equations and comparator identities were checked against the named primary sources. The supplied script is not automatically trusted or executed.

Separate worktree: `C:/Users/Chris/thesis-recovery/action-verification-testbed-20260922`; branch `action-verification-testbed-preparation-20260922`; base `d15d4aaf3e578b3ddc7f9bf236a8b69a7ed52075`. Initial worktree was clean. No historical scientific module is edited. LGP closure and continuation's working-system status remain unchanged.

`CONTRACT.json`, generator and decision code are committed before artificial execution. Every scripted test/reference attempt uses `bounded_run.py`: cumulative four wall-hours, 4-CPU affinity, 8 GiB job memory, no GPU, one-billion-byte output ceiling. Pure Python kernels are single threaded; native library thread environment limits are also set. The inspected scripts produce bounded JSON only (no unbounded external code). Maximum one run lasts 600 seconds; failures/timeouts are retained. Outputs use exclusive-create; rerunning the suite into the same directory fails rather than overwriting evidence. No dependency installation, simulator, checkpoint inference, Slurm, WSL change, historical archive read, or research-data training is part of this implementation.

Request attachment SHA256: `59e321dc75e3376c81c65f3a5eb7ff464928338862759e26de50b811694ebfcb`. Pre-execution contract/code commit: `a9fbc48` (full hash in Git history). The next real-runtime document is a proposal, not an executable approval. Deliver and stop.
