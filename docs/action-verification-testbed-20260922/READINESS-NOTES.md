# Final preparation review

Tests: 22 distinct final unit tests pass; the earlier20-test run and later22-test run are both retained. All18 fixed artificial scenario/seed combinations completed once. Metadata verification and role cross-check passed. The recorded six local runs consumed17.812 seconds total wall time, with maximum34,959,360 bytes aggregate Windows-job memory and affinity mask15 (four logical CPUs); pure-Python tests were single threaded. No timeouts, failed tests, GPU allocations, research fits or simulator runs occurred. Git diff whitespace check passed. The artificial Monte Carlo result is deliberately not a theorem test.

Review found and fixed an interface edge case before final unit testing: duplicate baseline-alias slots must share predictions and canonicalize back to baseline, not count as overrides. The original scenario outcomes were unaffected because those artificial banks have distinct slot identities. The original JSON arithmetic error in calibration expected sample count is documented, not hidden or outcome-tuned.

The sequential scenario is a specified **probability-tree stress example**, not a rolled-out learned robot policy. Its repeated-policy column defines repeating the same selected intervention in the shifted state (with the forecast unchanged), giving .10 success; following the fixed baseline tail gives .80. The separately executable native/checker-path unit tests verify actual-observation feedback and fixed-bank behavior. Real state-dependent repeated decisions remain untested and need the separate stage in REAL-RUNTIME-PROPOSAL. Do not read the arithmetic column as an empirical whole-policy measurement.

Real-runtime proposal details: six clock features are `[elapsed/150, remaining/150, 75/150, 15/150, min(15,remaining)/15, 1]`; no source ID, split ID, future state or historical outcome is a predictor input. Observed seven-dimensional state uses the accepted current-state ordering, to be authenticated at the interface gate. For the two binary single-decision primary contrasts, the predeclared directional test is exact one-sided McNemar (conditional binomial .5 on source-discordant pairs), followed by Holm over those two tests. The repeated native ACID/vanilla comparison is a separate later-stage family, not a third chance to select a favorable result.

Unresolved production work is not paper uncertainty: Le-WM image/tensor port, source fixture pins, primitive decoder parity and branch-clone independent endpoint validation must be frozen/tested before a runtime direction can be executed. No production loader/launcher is included. The requested next concrete protocol is supplied, but AV0 is not a claim that that protocol is already execution-ready.

No differentiated treatment exists beyond the controls. This is a reason to review what capability is missing before authorizing expensive collection, not a reason to invent a novel label. The long resource table is a worst-case reservation for separately directed stages; only the included four-source tranche is proposed as the first runtime action. Nothing advances automatically to repeated deployment.
