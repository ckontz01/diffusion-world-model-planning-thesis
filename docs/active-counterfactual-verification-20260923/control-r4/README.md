# ACV0 R4: exact pending-accounting placeholder recovery

This is a control-only technical continuation under the user's direct advance monitoring/recovery authority, committed at ace242ee5ea14af284401ad857fc3a49b30e1b58. It is not a new scientific experiment, fresh direct signature, or reasoning-chat approval. The copied MONITORING-AUTHORIZATION.md and exact INSTRUCTION.txt retain that authority and its limits.

R3 stopped on an immediately post-submission accounting row for304237:

```
304237|allocation|PENDING|0:0|0|0||gpu09||Unknown|superworld|
```

Its exact allocation ID was present, but the scheduler had not yet populated the submitted name or resources. The strict name check treated the literal placeholder `allocation` as a different task. The raw response and traceback are preserved. Later accounting shows the intended job acv0r3-collect-fit-1207 COMPLETED0:0 in157seconds. All37 successful task seals/technical/hardware checks authenticated; all38 allocations reconciled including historical failed304189/23seconds; no live, unknown or ambiguous work remained. Carried GPU charge4862seconds, CPUstage0. No outcomes, physics or models were decoded for the repair.

The only scheduler-behavior change allows that exact twelve-field GPU pending placeholder (with the exact submitted ID) to consume the existing incomplete-accounting allowance: eight15-second waits, then fail-stop on the ninth incomplete observation. It never accepts the placeholder as a completed task, supplies no zero accounting defaults and never resubmits. Other mismatched names/IDs, changed placeholder fields, terminal placeholders, unknown states, malformed terminal accounting and unavailable scheduler still fail-stop. Full terminal identity/resources/seals remain mandatory. This is intentionally not a universal workaround for arbitrary scheduler errors.

R4 reauthenticates all37 existing suppliers with current dated receipts and submits exactly the302 never-submitted original tasks, starting collect-fit-256. Successful304237 is never recomputed. Old304220 was accepted by R3 and is only reauthenticated here; history is not backdated. R1/R2/R3 ledgers, source/approvals, failed attempt and STOPs remain immutable. The new resolution binds all three exact STOP hashes; unknown faults remain blocking.

The full remaining GPU reservation154200 plus4862 historical seconds is159062, within220800. The original21600 CPUstage seconds, serial exact RTX6000Ada/gpu09,4CPU/24GiB GPU and4CPU/8GiB CPU resources, scientific grid/models/roles/settings/seeds/endpoints and worker caps are unchanged. Count all prior and new source/control bytes within500MB, live2GB and inclusive8GB. No new failed-task replacements or automatic retries are permitted inside this finite package. Expected final counts remain339 successful unique logical tasks and340 allocations including the one original failure.

Tests use synthetic files and mocked transport/scheduler only, never research payloads or allocations. All R2/R3/R4 testing shares7200seconds/fourCPUthreads/8GiB/250MB. R4 tests exercise the exact raw failure, finite wait then strict completion, adverse identity variants,37 immutable reused suppliers,302-only dispatch, unchanged fitting/evaluation gates, complete accounting and final whole/member archive verification. Every earlier test attempt remains recorded.

Finalization is exclusively finalize4.py for this lineage: validate the original339 scientific tasks and both historical observation faults, all charges and gates, and all nine source/control/run roots. Archive the new study once, including original evidence and all R1/R2/R3/R4 STOP/resolution/authority records. Use the original authenticated native-Windows transfer/member verifier, never the obsolete R1 archive entry point. Preserve partials on failure. Decode scientific results only after full acceptance and designated-SSD preservation. AV1, historical studies and E12 drafts remain untouched; no follow-up experiment is authorized.
