# ACV0 control-only continuation R2

Additional execution authority is the exact `INSTRUCTION.txt`, supplied under the user's standing delegation, not a fresh direct user signature. This resolves only the pinned R1 hostname-association controller fault. It authorizes zero replacement allocations, retries or scientific changes.

The R1 scientific source, original worker approval, logical grid, roles, models, analysis and all existing run/control bytes stay unchanged. `BASELINE.json` authenticates 125 existing files without decoding scientific outcomes. Existing successful 304193 supplies collect-fit-490 once. Its acceptance is a new dated metadata-only event, not a fabricated R1 event or a new allocation. The two exact hostname pairs retain the original strings and all hardware gates; no UUID equality across allocations is imposed.

`controller.py` is a new entry point. It never calls old dispatch.main. It serially submits only grid[1:] (338 tasks), starting collect-fit-545, with original source/worker approval and the separate R2 authority. Both loop and scheduler block collect-fit-490. GPU allocations are constrained to the authorized gpu09 node. The original 103 GPU-seconds carry once; the initial full reservation is 219,103 of 220,800. CPU stages remain 21,600 allocation-wall seconds. Any fault ends new dispatch without automatic retry. Logs, dated acceptance, resolution and append-only ledgers reside in the exclusive R2 control directory. The combined 339-task scientific view excludes the historical failed allocation; the campaign view includes all 340 allocations if completed.

Both included technical suppliers must pass before the third source. Both unchanged 192-update model fits must be sealed before the 32 final-development sources. Original analysis receives all 338 successful pre-analysis tasks in original order, not just the R2 segment. No outcome-dependent branching or scientific aggregate reading is allowed before completion and verified external preservation.

`finalize.py` separately authenticates the original resolved STOP, all old bytes, all new technical seals/allocation identities/charges, gate order, model identities, combined ledger and no new fault. It does not call R1 archive(). The one-shot new-study archive includes original scientific source/approvals, all successful outputs/models, prior-v2 provenance, raw R1 failure records, and R2 source/authority/acceptance/resolution/ledger/completion. Original STOP stays inside it. Unchanged R1 whole/member verifier and native Windows backup transport are reused, not the incompatible old archive entry point. No historical study is rearchived or retransferred.

Storage adds the entire R2 source/control footprint to the original 500 MB source/model/control/analysis envelope, which already reserves old failed evidence and CPU outputs. Full GPU output reservations remain 1,312 MB, live reservation 1,812 MB and inclusive four-copy allowance 7,248 MB (caps 2/8 GB). Per-job 10/2/100 MB limits remain. The designated THESIS_SSD identity and >=40 GB free are required; no deletions, overwrites, alternate disk, transfer retry or research rerun.

The preparation runner records every synthetic/scripted attempt under a separate cumulative 7,200-second/4-thread/8 GiB/250 MB budget. Tests perform no Slurm submission, model call, physics simulation or research payload decoding. Transport uses only configured SSH stdin and host Python 3.9.6; no dependency, permission, credential, security or WSL changes. `SOURCE-MANIFEST.json` freezes this control-only closure; the disabled template remains false. A separately enabled authorization binds its exact identity and original science. Only this new small package is backed up before continuation.

Historical monitors remain paused. No follow-up experiment, source substitution, protected payload access, promotion or automatic continuation after another fault is authorized.
