# ACV0 recovery R1 — one replacement only

This is a control-plane recovery under the supplied standing-delegation instruction, not a new scientific protocol or direct user signature. Bindings-v1/v2 and the failed v2 run remain unchanged.

Every GPU worker records and requires exactly `NVIDIA RTX 6000 Ada Generation`, one visible device and CUDA availability, before any checkpoint/model/reference/physics load. The partition remains `a6000`; the partition label is not a measured device name. Job304189 did not preserve its device name and is not retrospectively labelled.

`collect-fit-490` is attempt2: 1740 scheduler seconds, 1620 work seconds, 120 preservation seconds (supervisor stops at1730, retaining10 seconds before the scheduler limit). Its physical task and output cap are unchanged. All other338 task envelopes are unchanged. The first successful490 and545 are included fitting data and must pass the original technical-only gate before a third source.

Campaign allocation charges begin at23 GPU seconds for FAILED304189; initial worst-case reservation is220763/220800 seconds. There are339 new allocations,340 campaign allocations,339 unique successful task keys if complete, one authorized replacement and zero automatic retries. The failed attempt is never an outcome, label, fit observation or scientific denominator. Separate append-only campaign and successful-task ledgers enforce this distinction.

Prior source, approval, control, log and failure bytes are authenticated and copied under `provenance-failed-v2`, with originals untouched. Original bytes, provenance copies, external R1 control records and archive copies count in the existing500MB/2GB/8GB reservations. Final preservation includes R1 source/control/run plus separately labelled prior provenance; no historical LGP/AV0 archive is recreated or transferred. Any further execution or transfer fault is fail-stop, with no second replacement.

Scientific modules, input bindings, data roles, models, sources, optimizer updates, comparisons and analysis are unchanged. The grid hash is explicitly changed only for task490's time envelope. The false approval is preserved; a separate enabled recovery approval must bind the exact source and instruction.

Preparation uses a new7200-second cumulative scripted-test budget, four CPU cores/threads maximum,8GiB Windows Job memory and250MB new artifacts. Tests use fake hardware/schedulers, synthetic arrays and previously preserved artificial fit fixtures. No new efficacy grid, research fit, model/physics probe or GPU allocation is part of preparation.
