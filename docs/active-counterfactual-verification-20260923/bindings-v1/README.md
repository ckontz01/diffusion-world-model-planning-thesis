# ACV0 real-runtime bindings — execution disabled

This completes the production-binding preparation requested in **ACV0 REVIEW — COMPLETE THE REAL-RUNTIME BINDINGS; NO RESEARCH LAUNCH YET**, following reviewed commit `1c66038901fe39640add38aa796a816c06ccd1ae`.

The next decision is whether to launch the frozen, bounded pipeline. This is **not execution authorization**, evidence of robotics efficacy, a new architecture, or a claim that the artificial informative-case tie was repaired. Only an explicit execution instruction **here**, identifying this immutable package, can enable it. Earlier browser delegation does not override this gate.

## What is implemented

| Requirement | Concrete implementation and evidence |
|---|---|
| Actual pinned model/image/native-world bridge | `bridge.py`; seven authenticated source-code files in `INSPECTED-SOURCES.json`; mocked actual-loader order, BF16/image/time-layout tests |
| Fresh physical replay and all eight controllers | `episodes.py`; direct delivered-action recording; separate new World per branch/arm; no SAGE generator, LGP proposer or learned local target |
| Independent saved evidence and role checks | `verify.py`; raw states, dynamics, images/hashes, delivered actions, plans, clocks, same-step endpoint, allowlisted initial/H75 identity and whole-source boundaries |
| Explicit fitting amendment | `fitting.py`: exactly **192 full-source updates each**, batch64, seeds94011/94012, unchanged architectures/losses/preprocessing/Adam; final-update checkpoint only; validation16 reporting only |
| Serial finite execution | `dispatch.py`, `worker.py`, `supervise.py`, `run_worker.sh`; exact339 jobs, included490/545 tranche, model seal before evaluation, write-ahead reservations and no retries |
| Independent final analysis | `analysis.py`; all32 source-level outcomes for all8 arms, three primary comparisons, shared10,000 bootstrap draws, predictive diagnostics on actually observed selected branches |
| Preservation | `preserve.py`: one immutable archive, every-member/whole-archive verification, designated native-Windows SSD, exclusive partial retention and no transfer retry |
| Artificial completion tests | `test_bindings.py`, `completion-tests-v1/RESULTS.json`, `ANALYSIS-FOOTPRINT.json`, `ATTEMPTS.jsonl` |

All reviewed files outside this new directory remain byte-for-byte unchanged. In particular, the original seven-case neural fits, positive/negative/reversal results, prototype reproduction and sensitivity outputs were **not rerun**. AV0, its paired-improvement addendum, the historical studies and E12 drafts are untouched. AV1 remains unlaunched.

## One training amendment; otherwise the existing protocol

The earlier `../PILOT-PROTOCOL.md` remains historical. Its twelve-update real-fit proposal is superseded **only** by 192 optimizer updates for each model over all64 fitting sources. This is not described as twelve epochs. `GRID.json` removes `epochs` from the two fitting jobs and sets `updates:192`, `batch_sources:64`. Model sizes remain90,795 and106,177 parameters at D192; x996, a212, r192. Adam remains .001, (.9,.999), epsilon1e-8, global clip5, no weight decay; source shuffle94013. Preprocessing uses fitting sources only. `FIT.json` separates pre-update fitting loss, post-update validation reporting, and final fitting/validation loss. There is no validation-driven control flow, checkpoint selection or extension. The ordinary job also fits the frozen **Bayesian last-layer approximation**; no full ALPaCA or calibrated-safety claim.

The exact64/16/32 IDs, payload hashes, source keys and environment seeds are retained in the accepted roles file and bound in `INPUT-BINDINGS.json`. Reference indices are not inferred from trailing canonical attempt numbers. No protected CVL closed-loop, BP1 evaluation or1600–5999 payload was opened. The numerals used by artificial test fixtures are provenance/grouping labels only, not access to those research references.

Other fixed elements remain: H75 goal, native150-action budget, one initial verification opportunity, five+ten+135 physical schedule; maximum4×4 tree; N300/K30/J30 NumPy float64 CEM; no warm start, elite injection or extra pilot;128 antithetic stratified response samples per prefix; all8 controls and original seeds/arm rotation. Search settings are not CLI options. Reduced N4/K2/J1 is used **only in artificial interface fixtures**, never by production workers. Full production counts and worst-shaped output dimensions are tested separately.

## Inspected tensor and physical contract

The pinned native wrappers first resize rendered/goal RGB images to224×224 with PIL bilinear interpolation. The Le-WM transform then follows the inspected helper: NHWC uint8 → `[1,1,3,224,224]`, float32, divide by255 iff max>2, bilinear resize with `align_corners=False`, ImageNet normalization, then BF16. The seemingly unusual max>2 rule is preserved, not silently replaced. Actual source inspection found no incompatibility requiring a search/dtype change.

Loading authenticates the exact object checkpoint before deserialization, installs only the two pinned Le-WM pickle class mappings, and calls the inspected exact-path `AutoCostModel` branch. No directory search, network download, fallback model, generator or action prior is used. Frozen eval/no-grad BF16 parameters are fingerprinted before/after the job. The initial embedding is cached; each rollout uses a new dictionary and checks current-latent identity. Normalized actions are `[1,N,3,10]`; predictions are `[1,N,4,192]`, current first. Outputs become FP32 NumPy arrays, with the existing float64 search/cost arithmetic.

The original float64 tree/plans are preserved. The native Box receives their explicitly recorded **float32** conversions, and the wrapper's returned `info.action` must exactly match those delivered bytes. This is an interface representation, not a different historical Torch planner or scientific rounding search. Duplicate detection and tie rules in the accepted planner remain unchanged.

Each branch gets a new native fresh World/Space and replay from the original instantaneous initial record. No state assignment at a post-prefix checkpoint substitutes for replay. The original baseline and all selected node/action/prediction associations are saved. Canonical first-five RGB frames and initial/goal RGB images are retained; every other actual image has an exact typed-array digest. All post-action latent/state/proprio/dynamics/action/flag records are saved. Prefix replay compares physical bodies and velocities, forces/torques, dynamics constants, observations, flags and delivered actions, not merely latent similarity. Incidental wrapper IDs/timing and its random t0 action placeholder are not physical history; they are not fed to a policy or counted as delivered actions.

The verifier independently recomputes native success from the same post-action state: combined agent/block position norm<20 and circular angle difference<π/9. t0 is never a success. Native TimeLimit stays300; reaching150 is a budget end, not truncation. Early prefix success produces one physical execution and zero unused suffix labels/residuals. Every active suffix has its own fresh prefix replay and canonical response association. Every evaluation controller follows its own actual later observations. No held-out unexecuted outcomes enter search or deployment.

## Finite execution and interlocks

The strict false approval template binds source-manifest bytes, input bindings, exact grid/roles, run namespace, no-retry policy and caps. `Authorization` rejects it before research checkpoint/reference opens or scheduler calls. No supplied command generates an enabled approval. An approval file is a recorded execution interlock, not a cryptographic user signature.

Frozen grid:80 collection,2 CPU fitting,256 evaluation,1 CPU analysis =339 attempts if no fault. The first two jobs are fit sources490 and545 and are included, not additional. Their technical seals must pass before a third collection. All collection completes before fitting. The two final192 model/preprocessing seals and the Bayesian fit are frozen before opening final-development references. One allocation runs at a time; no retry, requeue, resume, replacement source, extra allocation or automatic follow-up exists.

GPU caps remain one A6000,4 CPUs/24GiB,1,800s collection and300s evaluation;220,800 allocated GPU-seconds total. Collection preserves120s, evaluation60s, within those hard caps. Three CPU jobs each4CPU/8GiB/7,200s remain21,600 **CPU-job allocation seconds**, not process CPU. Failure charges are written before the terminal-success check; ambiguous submissions/observations retain their full reservation and stop. The scheduler's actual allocation IDs/TRES are retained. Worker partial evidence, incomplete fitting weights/history and failure records are preserved without becoming accepted results or restart checkpoints.

Output caps remain10MB/collection,2MB/evaluation,500MB combined source/model/control/analysis,2GB new live,8GB live/archive/partial. Full future reservations are enforced, not just current bytes:1,812,000,000 live and7,248,000,000 inclusive. Worker stdout/stderr are each capped at65,536 bytes; overflow is a preserved failure, not silent log deletion. The maximum339 pairs total44,433,408 bytes. Slurm wrapper logs, manifests and allocation records are additionally included in the500MB control check.

Native launch uses the established container/environment with clean environment, headless rendering, deterministic CUBLAS settings, offline model libraries and read-only research/source mounts except the exclusive new run directory. No dependencies, permissions, credentials or account configuration are changed.

## Test results and scopes

`completion-tests-v1` exercises artificial collect64/validation16 → saved-evidence verification → source-grouped D192 fits → final seals → all8 evaluation paths → independent saved checks. The maximum fitting arrays are256×996 contexts and256×4×212 candidate features. Joint192 updates/checkpoint:11.797s; ordinary192 updates/checkpoint:11.000s, including its Bayesian fit. This is local CPU feasibility evidence under the bounded runner, **not a measurement of cluster GPU throughput or a robotics result**.

Complete worst-shaped uncompressed collection output:6,320,520 bytes; evaluation:1,343,426 bytes. These include trees, full150-step traces, raw canonical prefix images, metadata, extra69,632 bytes of metadata/seal reserve and actual seals. A mocked full338-preanalysis/256-cell report with a conservative65,536-byte technical record for every preceding worker serializes to22,409,903 bytes including100KB seal reserve. Together with maximum worker logs and a30MB source/model/fit allowance, this is96,843,311 bytes, below500MB. Actual production guards remain authoritative; oversized output fails instead of dropping evidence.

The final integration suite includes12 test groups: actual-loader call order; image/BF16/time layout; D192 checkpoint/role/history checks; terminal prefixes/suffixes/full budget/t0; saved corruption/cross-prefix rejection; all8 execution paths; common integration samples and distinct actual future observations; disabled authorization; all339 mocked allocations/gates; ambiguous submission and failed-job charges; mid-branch fault preservation; all256-cell independent aggregation; archive member hashes/exclusive writes. No performance win is required. The test runner pins four CPU affinity bits, one BLAS thread per process, an8GiB aggregate Windows Job limit and a14,400-second cumulative budget; all attempts are append-only in `ATTEMPTS.jsonl`.

Genuine remaining runtime uncertainties are actual pinned GPU/renderer startup, GPU throughput under the unchanged caps, and deterministic physical common-prefix replay on the live pinned physics implementation. These cannot be empirically certified without the currently forbidden research model/physics execution. The included490/545 technical tranche is the frozen place to test them after explicit launch authorization. Failure means stop and reconcile, not raise caps or alter scientific settings.

## Future entry points — do not execute research now

`source-export.tar` is the verified thin transport containing the source closure, its accepted-code dependencies, manifest and false template, in the original repository-relative layout. `SOURCE-TRANSPORT.json` authenticates every member and the whole archive. It has not been staged on the cluster. Do not copy a partial directory that omits the accepted parent modules or fresh-initialization module.

On the approved immutable cluster source snapshot, `dispatch.py --approval <separately enabled approval> --run <exact template namespace>` is the only campaign entry point. It calls `run_worker.sh` → `supervise.py` → `worker.py`. All reject the supplied false template. `preserve.py archive --approval ... --run ...` requires all339 completed/sealed jobs and no STOP; `preserve.py backup --request <exact remote BACKUP-REQUEST.json>` is the native-Windows one-shot transfer to the designated THESIS_SSD volume with40GB free. Failed streams retain their partial and require explicit scoped recovery. No old archives are retransmitted.

Scientific aggregates remain closed to human/agent interpretation until compute completion **and** prescribed whole/member backup verification. The final report is already independently produced inside the last CPU job and sealed into the archive. It reports all8 arms, all32 paired sources, gains/losses/ties and the three primaries C−ordinary, C−Bayesian, C−early-replan; C−vanilla/A/B/D are supporting. Shared seed94301 bootstrap intervals, including Bonferroni98.3333%, are **nominal small-development summaries**, not a finite-sample proof of simultaneous coverage. No selection, safety, novelty or promotion claim follows automatically.

The new preparation-only backup is separate: `backup_package.ps1` copies **only this small new directory** to `D:/THESIS-BACKUPS/active-counterfactual-verification-20260923/bindings-v1`, verifies every byte/member and emits `DELIVERY.json`. It does not repeat the accepted preparation backup or historical archive cycles.
