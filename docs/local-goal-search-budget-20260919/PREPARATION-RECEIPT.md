# LGP-RB1 preparation receipt

Prepared after LGP1 acceptance; finalized20September2026. **No research
execution authorized or launched.** Continuation remains the baseline and
neither proposer is promoted. Historical result
`f42c189b2e18968303dee5a733cf670d2301a657` and all its artifacts are unchanged.

## Delivered implementation

The sole scientific change is the explicit total scored-population budget
in the shared policy:1/5/30. The existing FP32 CEM implementation, decoder,
projection, 300-candidate population, 30 elites, stable tie handling and
final-elite-mean return are reused unchanged. New sidecar paths implement
the exact grid, independent complete analysis, technical compatibility,
exclusive serial dispatch, disabled authorization and union preservation.
No fit/optimizer/training stage is reachable through this new dispatcher.

New read-only initial-bank fingerprints and setup/episode timers are
instrumentation, not inputs or a selection mechanism. Protocol§7 states
their timing boundaries and overlap. The run's native endpoint verifier
and physical action evidence are unchanged.

## Compatibility established and limits

- Byte authentication directly on Prometheus: nine unchanged scientific
  modules, six exact final checkpoints, executed source manifest and original
  all-six freeze — **17 matches**. See `REMOTE-BYTE-COMPATIBILITY.json`.
- `REUSE.json` binds all192 original30-population worker seals, original
  source/approval provenance and384 outcomes. Input/role/model roots remain
  unchanged. Old30 outcomes are explicitly marked reused, not freshly run.
- Artificial tests compare the actual executed legacy Policy against the
  new default30 Policy, including exact actions and round summaries for both
  proposer families, common first/later-stage banks at1/5/30, n−1 refinement
  draws, prefixes, ties and final-elite-mean return. No research model load.
- LGP1 retained context hashes, round summaries, elite indices, timing and
  endpoint states/actions. It did **not** retain raw proposal banks or their
  hashes. Historical raw-bank equality is not established or claimed.
- Future strict-CUDA research-model compatibility remains untested. The
  proposed two jobs make16 first-decision calls, deliver zero actions and
  evaluate zero complete episodes. They must pass before new main runs.
  Subsequent policies follow their own actual trajectories, not old30
  trajectories or forced shared future actions.

## Exact counts and proposed launch envelope

| Quantity | Fixed proposal |
| --- | ---: |
| Frozen models / optimizer updates | 6 reused / 0 new |
| Independent development sources | 32 unchanged |
| New main episodes | 768, at1/5 populations |
| Historical main episodes | 384, at30 populations |
| Combined main episodes | 1,152 |
| Main / compatibility GPU jobs | 384 / 2, serial A6000 |
| Final CPU analysis jobs | 1 |
| Maximum new GPU allocation | 70,320s =19.5333h |
| CPU analysis allocation | 4CPUs/8GiB,7,200s |
| New worker / source-control-log caps | 2GB /100MB, decimal |
| Inclusive remote cap, history and final archive included | 8GB, decimal |

Each main job contains both horizons. Maximum full-budget new main physical
actions are172,800. No extra allocation or automatic retry is permitted;
failed/technical allocations remain charged and preserved inside these caps.

Measured LGP1 setup/other overhead is retained rather than dividing total
runtime by the round count. New main estimates are3.471h with only refinement
scaling, or5.430h with no refinement speedup, excluding at most0.333h for
compatibility. A2× no-speedup scenario including that technical reservation
is11.193h. The19.5333h hard GPU ceiling includes all386 allocations at their
limits; the2h CPU analysis cap is additional. Host verification, archiving
and transfer are separately costed in `RESOURCE-PLAN.md`, not concealed as
zero overhead or counted as GPU allocation. These are planning scenarios,
not measured shorter-budget runtime or a promised completion time.

## Tests and source package

**33/33 passed** from the final exported source in the pinned runtime:
Python3.11.10, Torch2.5.1+cu121, NumPy2.2.6, one CPU thread, strict deterministic
algorithms, no `--nv`, CUDA not initialized. Test wall9.930s, process CPU
9.344s, peakRSS425,979,904bytes. Detailed named tests are in
`TEST-RESULTS.json`. These include the old pinned criterion/decoder/endpoint
regressions and full legacy lifecycle, plus the new actual policy/driver
paths at1/5 with artificial tensors and vector-pool methods. The source's
disabled approval was tested against all three execution entrypoints.
The dispatcher/seal/preservation imports also pass without site packages;
`bash -n` passed for the new wrapper. No optimizer, physics or pretrained
neural forward pass occurred. The earlier synthetic export also passed33
tests and is preserved separately; the final receipt binds the final hash.

Immutable export on the verified external SSD:
`D:/THESIS-BACKUPS/local-goal-search-budget-20260919/preparation-v1`

82 archive members,1,617,920 tar bytes. Every copied member and the complete
archive were verified. `PACKAGE.json` contains the member inventory and:

| Identity | SHA-256 |
| --- | --- |
| New source manifest | `a0bcdb48029909e0b54a86d8b59c54fc4fa60a3734a9634c761e5caea4c9778b` |
| Source archive | `afa8494d0be713dc03385cac89dc706e9b9959bbe959e80319a834be21eea5f7` |
| Unchanged input lock | `b21bac97520696b8bb2008a3cfef3f5188d4c59dd117bc158cc2fe2e86ff5aaa` |
| Protocol | `69ae04db8d820da250a29fef73fa20e71d7b1ca1708c4a2846fba0055e9b20ee` |
| Reuse lock | `8dfbe2b7115e981f1198cb65f83de58d1d77a3ac7f91d4d0294aa7d7163df0eb` |
| Exact grid | `7014a7a101ec0da2e48d983575170f1c34a87a5f29b97243f898091b7ba7bf41` |
| Disabled approval template | `2a4c6952194acb6045bbbb8f2983aebd4043fb35f3ac5f5deddfdd888a21716a` |

The exact export was copied into
`/lustreFS/data/superworld/ckontzias/thesis/staging/lgprb1-preparation-tests-v1/source`
for CPU-only synthetic tests; this is not an execution snapshot or a launched
research namespace. Test evidence and this receipt sit outside the sealed
source to avoid modifying its manifest or introducing a self-hash cycle.

The existing WSL `scp` executable reported its separate I/O fault during
transport. No WSL filesystem, account or permissions were repaired or
changed. Source-only transport used the already functioning configured SSH
connection instead, writing exclusively to the new Prometheus staging root.
The healthy recovery checkout and verified THESIS_SSD were used; no bulk
laptop-storage fallback. E12 drafts and every historical artifact were left
untouched. Final test-source authentication also detects unsealed additions.

## Readiness and bounded interpretation

This is ready for the single launch-envelope decision. No enabled approval,
new research output, GPU submission, new labels, reserved-reference access,
model promotion or follow-up experiment exists. The comparison can establish
budget dependence on these exposed sources and frozen models, not untouched
confirmation or universal proposer superiority. All budgets and source-level
effects will be reported, without selecting a favorable budget as a result.
