# CVL-1 controller continuation launch receipt — 15 September 2026

The [authorized continuation contract](CONTINUATION-20260915.md) is implemented
and published at commit `5dd6d487582d18c659f4c904ffffeedc41fea904`; the remote
branch hash was verified. All **62 local regression tests passed in 39.228s**.
The **seven continuation tests passed again in 8.644s** from the LF-only exported
controller package, importing the original frozen contract/backup implementation.

## Exact authenticated identities

|Artifact|SHA-256|
|---|---|
|Controller-overlay manifest|`2b6e81a76aefe5943872bf2f01e5561e75595f1b032b0eba431b258ed373e03b`|
|Controller Python bytes|`f75c0ff3c95af9da8197cc0b0218856f7dd8c81d2d6b36d8f8fe9f389925128a`|
|One-use continuation approval|`c7707df72db69c2124c9ed576cb24104518b848b234cab30b65954d902b973ae`|
|Original scientific source manifest, unchanged|`521a0e6627c570ffa30d12adc63f92cf9ba76dee2c9bc4c59d24f4982f4f45f2`|
|Original protocol, unchanged|`6451cbd44e3fdfd430b3f6d1c69eb49a14bb493beaa1acdedee971a73978ff2c`|

Immutable controller overlay:
`/lustreFS/data/superworld/ckontzias/thesis/snapshots/candidate-value-controller-20260915-2b6e81a76aefe594`

Separate approval/readiness/launch control directory:
`/lustreFS/data/superworld/ckontzias/thesis/staging/candidate-value-resume-20260915-2b6e81a76aefe594`

The original run remains:
`/lustreFS/data/superworld/ckontzias/thesis/experiments/candidate-value-learning-20260914/run-521a0e6627c570ff`

## Verification and preservation

Before launch, the original complete source manifest and all pinned runtime-file,
environment-tree and code-tree hashes passed authorization. All 83 completed
jobs matched fresh Slurm COMPLETED/0:0 accounting, their original exact task
coordinates, report technical identities, adjacent seal hashes and every sealed
member. No later stage or train-81 artifact existed. The continuation approval
records each job, elapsed allocation and seal hash individually.

Reconciled cost is exactly **17,151 GPU seconds**, including failed preflight
301159 and completed train-80 worker 301256; CPU fit/analysis cost is zero. The
original dispatch-log checksum matches the preserved stop record. No prior
scientific outcome, bank, trajectory, label, or fitted metric was interpreted.
All prelaunch verification is non-performance authentication/accounting.

The controller package, approval and verification receipt are content-verified on
the external SSD at:
`D:/THESIS-BACKUPS/candidate-value-learning-20260914/controller-5dd6d487582d/`

The original frozen exported worker source was also reverified there. The
manual stopped-run backup remains unchanged. The three E12 draft files remain
untracked in the original WSL repository and were not edited or staged.

## Backup service and controller launch

The old idle watcher was absent, with empty stderr and no requested or attempted
stage backup. The guarded restoration verified an empty archive destination,
no stage requests/acknowledgements and no final decision before creating its
exclusive watcher claim. It uses the original frozen transfer implementation.
New watcher: WSL PID **2043**, hidden Windows WSL launcher **17104**. No failed
transfer was retried. Its logs are `backup-resume.out/.err` beside the external
controller package; the original watcher logs remain untouched.

Volume D was verified as `THESIS_SSD` with **379,082,346,496 free bytes**. The new
exact-source readiness receipt is in the separate control directory. Stage
archives still go to the original external run directory, never C:/OneDrive.
Readiness is not a claim that uncompleted stages are backed up.

At **2026-09-15T04:19:35Z**, one controller process was started on Prometheus,
PID **2081952**, with original source/capsule/approval arguments and the separately
pinned continuation approval. Its startup repeats full authorization before
claiming the continuation or submitting any job. New events use
`DISPATCH-RESUME.jsonl`; technical stderr/stdout use `dispatcher-resume.log`.
Original `DISPATCH.jsonl` and the old staging `dispatcher.log` remain unchanged. Process creation
alone is not evidence of a worker submission or successful collection.

The next registered coordinate is train index 81 (reference 219, H150). The
original sparse-support, ranking-promise, backup and budget gates are unchanged.
No completed worker may be resubmitted, and no automatic retry exists.

### Confirmed first resumed submission

At **2026-09-15T04:19:59Z**, the new ledger recorded exactly train index 81,
reference 219, H150, submitted as **301257**. Slurm subsequently reported RUNNING
at 65 seconds. This is not yet a terminal cost or scientific result. The original
dispatch checksum remained `effb2591eb3eb9be4161d05e9aafb39e390d5bc61822532dc7794ea876d7019e`
after submission, and the restored watcher error log remained empty. The active
worker holds the unchanged 600-second reservation; completed costs remain 17,151
GPU seconds until new terminal accounting arrives.

The existing ten-minute monitor `monitor-cvl-1-approved-staged-learning` was
updated in place and reactivated for `DISPATCH-RESUME.jsonl`, the separate
controller/approval identities, carried-forward costs and restored watcher.
It stays quiet on unchanged/non-actionable state and cannot submit another
dispatcher, retry workers, patch frozen source or change scientific gates.
