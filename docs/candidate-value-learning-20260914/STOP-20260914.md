# CVL-1 real preflight stop — 14 September 2026

**Stopped before model construction, selected-reference access or collection.**
Do not retry, alter the running snapshot, infer a learning result, or advance stages.
The approved scientific scope and the focused saved-label correction are preserved.

## Actual execution

Frozen source commit `f93784550e2e83f5f6b7df247d2d902114ba5e12` and all exact
source/capsule/approval identities are in [the launch receipt](LAUNCH-20260914.md).
The 52 local and 52 exported-source tests passed; those are synthetic results.

|Registered stage|Actual state|
|---|---|
|Preflight 0, Slurm **301159**, gpu09|**FAILED**, exit **1:0**, 45 allocated seconds|
|Preflight 1|Not submitted|
|Eight-job technical collection tranche|Not submitted|
|Remaining collection, fitting, ranking, closed loop|Not submitted|

The worker failed in `candidate_value_contract.authorize`:

```text
RuntimeError: Runtime tree changed:
/lustreFS/data/superworld/ckontzias/thesis/envs/hi-lewm-artifact-py311-cu121-swm006
```

The host-side capsule freeze and dispatcher authentication passed, but the
worker's container-side environment-tree authentication did not. The observed
fact is a digest disagreement across these checks. This does **not** establish
whether bytes changed in time, the host/container filesystem views differ, or
another technical cause applies. No speculative root cause or relaxed check is
substituted. The failure occurred before the worker output directory, PyTorch
import, backend construction, selected-record load, or preflight tests.

## Secondary failure while preserving terminal evidence

The dispatcher detected the exact failed job and stopped without submitting a
replacement. Its terminal routine wrote `terminal/REPORT.json` with decision
`stopped_preserved_no_retry`, then copied the capsule bytes but failed while
`shutil.copy2` propagated filesystem extended attributes:

```text
PermissionError: [Errno 13] Permission denied:
.../run-45151f84a77190fc/terminal/LAUNCH-CAPSULE.json
```

Consequently there is **no terminal sha256.txt, no DISPATCH-FINAL.json and no
automatic terminal backup acknowledgement**. The partial capsule's content hash
matches the authenticated capsule; that is not a completed terminal seal.
No permission was changed, failed destination overwritten, or frozen code patched
to force the routine through. Both startup control logs and partial artifacts remain.

The earlier missing-parent startup problem, before any sbatch call, is separately
documented in the launch receipt. Creating the intended experiment parent and
starting the dispatcher again did not retry a research allocation. After the
real 301159 failure, no dispatcher or research-job restart was attempted.

## Charged resources and denominators

- One A6000 allocation, 45s = **0.0125 GPU-allocation hours**, including failed
  setup/authentication. Four allocated CPUs ×45s =180 core-seconds (0.05 core-hours).
- CPU fitting/analysis jobs: **0**, CPU-job allocation wall-seconds **0**.
- Slurm batch sampled MaxRSS: **91,024 KiB =93,208,576 bytes**. This is not node RAM
  or GPU-memory usage. PyTorch device peak was not recorded because import and
  model construction were not reached.
- Selected references opened: **0**; banks **0**, candidate-tail outcomes **0**,
  models fitted **0**, ranking-validation banks **0**, closed-loop episodes **0**.
- No control outcomes, reference-level effects, sparse-support decision or
  ranking-promise decision exist. They are **not evaluated**, not negative findings.
- Five preserved run files total **59,875 logical bytes**, after partial terminal
  copying. The terminal REPORT's `bytes=2537` was an earlier pre-copy snapshot,
  not the final directory total. Source/staging/control remain additionally
  covered by the existing 50MB control reservation within the 20GB ceiling.
- Queue/control/authentication and transfer time are distinct from charged Slurm
  allocation time; no zero-cost claim is made for host work.

## Verified independent failure backup

After the automatic terminal-copy failure, the five existing run files and the
two dispatcher logs plus BACKUP-READY.json were copied read-only to the approved
external SSD. Every copied content hash was compared to Prometheus. The source
tar, capsule, bound approval and researcher approval text already have verified
copies in the same directory. No research payloads were downloaded.

External root:
`D:/THESIS-BACKUPS/candidate-value-learning-20260914/source-f937845/`

Run copy: `stopped-run-copy/`. This is a **manual content-verified failure copy**,
not a successful automatic sealed-stage receipt. Original remote files remain
unchanged. The now-unneeded WSL backup companion (PID662, Windows launcher10340)
was stopped after these copies were verified; no monitoring automation was created.

|Relative file|SHA-256|
|---|---|
|stopped-run-copy/DISPATCH.jsonl|`f64407bf86a54096ed5d7972f05cd7b3990987f8c668b4238b35530d6ace97e3`|
|stopped-run-copy/slurm-preflight-0-301159.err|`04456a564608ba802659b142c4716d7f15c4d845e5250cd76172cc5664d65356`|
|stopped-run-copy/slurm-preflight-0-301159.out|`36c7b88f51c836f574ec8f37c3e465316fd6cf50fb43a2a25788751ebc5b3acb`|
|stopped-run-copy/terminal/REPORT.json|`7f9cd98a442ea3f7bcc580381030b6714f0ed7b6af19961f2558f0e97a0492e4`|
|stopped-run-copy/terminal/LAUNCH-CAPSULE.json|`6c18314f167ba54fe5306541bef10cf52b75ec61458b86ea05a89ff84d11f33b`|
|dispatcher.log|`bd8215e8c92068cdb4c4c658ff4e23afa51f44d1796496ccce890a8c6d27e5db`|
|dispatcher-start2.log|`965854817ccc582fd6468951419cf975127029f70e9d6338a3eb02edc66152ef`|
|BACKUP-READY.json|`3f0d08c0aed162ffda94bac72e665e7f0f1028c1055ea52a9bb58439fca38c1c`|

## Required next boundary

Report the real technical mismatch to the researcher. A separately authorized
bounded diagnosis may compare authenticated host/container runtime views and
inspect the copy2 extended-attribute behavior. This report does not authorize
changing pinned environment bytes, bypassing authentication/permissions, adding
jobs, retrying preflight or patching the frozen snapshot. No scientific redesign
is implicated by the available evidence. Historical results and the E12 drafts
remain unchanged; protected and 1600–5999 payloads were not accessed.
