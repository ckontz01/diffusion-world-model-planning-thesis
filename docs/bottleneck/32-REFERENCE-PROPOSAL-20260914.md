# Measured 32-reference proposal — NOT launched

Recommendation: if further diagnostic evidence is wanted, extend the **same
short-branch design** to the already identifier-selected32 exposed references,
reusing the completed four where source/protocol compatibility is preserved.
Do not redesign diffusion from one favorable local counterexample. This proposal
requires approval before changing the runner allowlist or submitting more jobs.

The fixed list is in [BRANCH-PILOT-SELECTION.json](BRANCH-PILOT-SELECTION.json).
That original file remains a historical planned selection, not a current execution
ledger. Only its first four have executed. All32 lie within exposed0–1599.
Retain H75/H150, checkpoint7201, anchors0/30, two repeats, the64×8 banks, four
conditioning contrasts, fixed scoring context, greedy64, stopping/tie rules and
common-noise construction. No outcome-based case replacement. Unavailable anchors
remain unavailable. No long-budget tail or new endpoint is included.

## Workload estimate

| Quantity | Measured4 refs | Full32 linear estimate | Additional28 if four reused |
|---|---:|---:|---:|
|Fresh-process jobs|8|64|56|
|Runner wall (mixed CPU/GPU, excludes construction/report/seal)|5.643min|45.147min|39.504min|
|Slurm allocated elapsed, excluding queue wait|9.05min|72.4min|63.35min|
|Saved banks/reports/seals|76.764MB|614.113MB|537.349MB|
|Physical branches|2208|17664 maximum|15456 maximum|
|Primitive steps including prefix replay|68360|546880 linear;549248 maximum|478520 linear;480592 maximum|

Step maxima follow the actual69-branch contract,15/30-action caps, anchors0/30,
and at most31 main-world actions per horizon. They are not estimated from success.
Linear times are planning inputs from four records, not guaranteed runtimes.
Different termination, model initialization, filesystem load and scheduling can
change the rate. Queue wait is unknown. Two repeats check repeatability; they do
not double the independent reference count. GPU memory figures in the pilot
are PyTorch allocator peaks, not total VRAM.

For review, budget **up to2 aggregate GPU-allocation hours and1GB new artifact
storage**, one A6000 allocation at a time,4CPUs and the existing24GB RAM request.
Retaining the request avoids silently changing allocation policy based on a small
sample. This is a conservative planning envelope, not an enforcement already
installed. A future launcher should use a5-minute per-job cap and stop new dispatch
at the aggregate envelope for review;64×5minutes would otherwise permit5h20min.
No automatic retries or size expansion. The additional28 estimate leaves roughly
56minutes headroom below2hours; timeouts preserve artifacts and require diagnosis.
Freeze the chosen envelope and interruption handling **before** that launch.

## Scientific decision it can support

Estimate the distribution and consistency of same-bank selection changes, adapter
and latent errors on generated actions, and short physical margin/success effects
across32 development references. Keep both horizons/anchors paired within each
reference; report reference-level uncertainty descriptively, not as new registered
confirmation. Inspect unfavorable and joint-interaction cases. Retain GMM as a
serious future method control; it is not replaced by this diagnostic greedy64.

The grid cannot establish distant-goal coverage, irreversible failure, a resolved
SAGE gap, publication novelty, or the best new architecture. Actual successor
information is diagnostic oracle input, not a deployable method. Any full-budget
tail, model redesign, extra checkpoint blocks, training, or new confirmation
requires a separately specified and approved study.

Remaining choice: approve this exact additional28 design/envelope, request a
different explicitly costed diagnostic, or stop at the completed pilot. None is
automatically selected by the pilot's isolated success. Historical source,
models, results and stopping decisions remain unchanged.
