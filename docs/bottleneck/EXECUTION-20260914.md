# Reviewed execution specification — 14 September 2026

This outcome-informed development assignment adopts the researcher's supplied
`thesis-worker-handoff-20260914.md`, not superseded branch/offline instructions
in the 13 September package. Branch: `bottleneck-diagnostics-20260913`; base
`001aad99a2e2e6a141797e0c516604e7188d706a`. No historical decision is amended.

## Receipt and immediate checks

Package SHA-256: `c242f770a4dddc78e90bd23d40cb29860000439616ce0a3ea73216219efada67`.
Patch SHA-256: `ee8e6eb4e8a6f5b399071c496207491aed9177b7405306dca48b5f8a12e788f5`.
All 24 manifest entries verified, all 25 archive paths checked for traversal and
duplicates. `git apply --check` passed on the clean base; the 18 additive files
were applied once. Original package reports remain historical evidence.
The 51 supplied tests passed in existing WSL Python 3.10.20 / NumPy 2.2.6;
the command wrapper subsequently returned an empty shell-status error, not a
test failure. The independent outcome command and shell syntax check separately
returned zero. All 15 pairs and 20 contrasts/cluster SEs reproduce; bootstrap
quantiles are not independently validated by that second implementation.

The recovery checkout was clean; original `/home/chris/thesis` still contains
three untracked E12 drafts and is not edited. D: is THESIS_SSD, reporting
390,871,556,096 free bytes; C: reports only 11,574,734,848. New bulk storage and
verification receipts go to D:. Filesystem health reporting is not a complete
integrity certificate. WSL read access and existing strict-host-key SSH to
Prometheus work. Old E14 pending jobs are unrelated and untouched.

## Preservation and reduction

Use a fresh CPU allocation in the existing pinned Apptainer/py311 environment.
First run synthetic tests there. Validate INPUT-LOCK, config, registry, protocol,
historical source manifest, compact outcomes and separate model byte identities.
Rehash every sealed stage-0 payload in all 450 tasks. Export a canonical receipt
of DONE/result/seal identities through authenticated SSH, then rehash the local
copy against it. A local index alone is insufficient. Leave old copies intact.
Only reference payloads 0–1599 may be interpreted. Metadata for the full
collection is permitted; later reference payloads are not needed here.

Before complete reduction acceptance, additionally check the original requested
start, horizon-specific goal, reference attempt/planner RNG seed, exact task
identities, per-arm/horizon/seed counts, and native truncations separately.
Recompute the physical predicate independently from original source semantics:
one joint four-coordinate norm <20 and wrapped angle <pi/9 at the same
post-action step; t0 excluded. Noncanonical angles or disagreements block
acceptance. Technical-invalid runs are never converted to planner failures.
Episode-loop times exclude initialization; solver-call times have the historical
synchronization/encoding boundaries. No end-to-end or equal-compute claim.

## Four-reference pilot contract (requires preservation/reduction acceptance)

Recompute SHA-order selection: references 1269, 582, 525, 722; H75/H150;
checkpoint block 7201; anchors at elapsed primitive actions 0 and 30.
Unavailable/terminated t30 anchors are reported, never replaced. Restore R3
initial request and replay all historical delivered actions. Restore planner
calls, schedule, action buffer and RNG consumption; physics replay alone is
insufficient. Compare archived selected-plan hashes and delivered-action bytes.
Fresh-process repeat replay must agree. Raw historical banks are not presumed
saved; newly saved/repeated banks are explicitly regenerated evidence.

Use one 64-first-candidate bank and one stored 64x8 continuation-noise bank;
same bank for greedy-64 and unchanged best-two-mean continuation selection.
First duration =15 primitive actions. E18 delta = H-(elapsed mod H), continuing
the unchanged H-cycle schedule, and overall episode budget =2H. Continuation
uses delta-15 and duration15 only when delta>=30; otherwise immediate scoring.
Remaining physical budget=2H-elapsed. Natural termination/truncation always
stops a branch; a first-chunk success is counted without post-terminal steps.

Primary intervention changes only SECOND-PROPOSER inputs: predicted latent/
predicted adapter state; predicted latent/actual state; actual encoded latent/
ORIGINAL predicted adapter state; actual latent/actual state. Scoring starts
from the original predicted LeWM successor in every primary condition. Raw
actual state and latent use pinned checkpoint mean/std exactly once; adapter
outputs already have normalized state units. Planner actions use the existing
decoder, not probe-fitted scaling. Mixed conditions may be inconsistent;
report joint effects/interaction without a single-cause claim.

Record full raw/planner actions, noises, predicted/actual latents and states,
all scores, selected indices, physical delivered actions, identity and timing
receipts. RNG snapshots restore the baseline post-first-bank state separately
for each condition; no condition consumes another's stream. Independent checks
must verify frozen inputs and exact generated banks, normalization, selector
arithmetic, replay boundaries and no post-terminal action. Concrete numerical
tolerances and physical rollout inventory will be finalized in the committed
runner specification BEFORE any new intervention metrics are inspected.

Initial resource ceiling: one A6000, <=2 hours per pilot job, at most four
references, two horizons and two anchors, with fresh-process repeats; measure
actual resource use before proposing 32-reference expansion. No broad grid is
authorized by this ceiling. A long-budget tail-policy study is explicitly
UNRUN and excluded from this first short-horizon pilot. Its policy and cost
require a separate committed specification. Short-branch failures do not show
irrecoverability or distant-goal coverage.

No training, architecture change, diffusion tuning, SAGE-grid restart,
confirmation access, protected outcomes, historical edits, permission changes
or connector work. Send a factual WORKER REPORT for independent review.
