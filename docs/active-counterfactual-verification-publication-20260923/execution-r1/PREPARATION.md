# R1 preparation and authority

The exact user-supplied recovery direction is preserved in `RECOVERY-INSTRUCTION.txt`. It is recorded direction under the standing delegation, not a fresh direct user signature. It authorizes exactly one task490 replacement and the original fixed remainder, with no subsequent retry.

The metadata-only23September reconciliation authenticated the sole FAILED304189 attempt (exit1:0,23GPU seconds, gpu09), complete old source/control/run member bytes and the inactive PID550069/start892062738. No other ACV0 allocation or live attempt was present. No scientific results were read; the failed attempt did not measure its device name.

Implementation is separate in bindings-r1. All scientific modules/input/role bindings remain unchanged. The only grid amendment is collect-fit-490 seconds1800→1740. The new exact-device interlock, historical charge/provenance checks, distinct ledgers, campaign final acceptance and storage accounting are recovery control-plane changes. The estimator remains byte-identical.

Focused test attempt01 preserved19 passing groups and one failing fixture: the synthetic scheduler string incorrectly added a second terminal pipe to an already pipe-terminated recorded row. The fixture was corrected without changing production validation. The entire20-group suite is repeated under the separate cumulative two-hour preparation budget. This is artificial testing, not another research attempt.

The corrected20-group integration suite passed. Binary transport roundtrips, overflow rejection and all three disabled-before-dependencies entry points also passed. The three test attempts used95.766 cumulative wall-seconds; maximum job memory172298240bytes, affinity mask15 (four CPUs), and less than1MB preparation output through transport testing. No research allocation occurred during preparation.

Frozen source manifest: `c2c6fcbe8c41fed22d0fde17bcb5f8cd456a74c1cb0d77039f4df94d86e0d468`. Source archive:593920bytes/44members, SHA256 `79a588f278282c1cbcb2a8c9daeffd26fa44abba16c31d1641c6e9035c73b898`. Old canonical grid `f9b4986c9f674122c253cff9aaf802e8d4b181180553d0e5643c0c92bb175a2a`; new canonical grid `2b893c8a1c13eb2e0add6d80b61814c573b884f94fabfbde89e05804950298f5`.

The metadata/runtime preflight authenticated73 existing runtime files, the existing container and Python3.9.6/Slurm controller pins. The existing worker Python3.11.10/NumPy2.2.6 interpreter was checked CPU-only, with zero checkpoint/reference reads or GPU allocations. The sole prior allocation matched and all new namespaces were absent. Native THESIS_SSD identity and40GB free-space requirement passed. Separate enabled approval and delegation provenance are recorded outside the source closure, leaving all false templates untouched.

Source transport must authenticate all members before the one replacement is submitted. A hardware-name pass alone does not establish live renderer/physics correctness; the included490/545 technical gate remains mandatory before a third collection source.
