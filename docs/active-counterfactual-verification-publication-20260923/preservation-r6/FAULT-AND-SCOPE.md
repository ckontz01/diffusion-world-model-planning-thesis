# R6: archive-root mapping correction only

R5 final acceptance succeeded with all339 scientific successes and340 allocations; completion pointer SHA a3df3959960154cd7e39d4f0868488142ddc2b8f388344a9c4947609d2b30f59. R5archive invocation then failed before creating final-preservation: the baseline's R4source root was the full snapshot, while the older inventory routine used the nested package root. The explicit membership assertion correctly blocked a mismatched label. No archive or transfer existed; read-only ARCHIVE-FAULT-RECONCILIATION.json authenticates that state and unchanged completion. Original R5fault/stdout/stderr are frozen as R5-ARCHIVE-FAILURE.json.

R6 replaces only archive member enumeration: each of the nine captured old roots is enumerated from the exact recorded root, retaining its relative paths. R5source/control and R6source/control bring the archive to13roots. No guessed prefix stripping or missing-member waiver. Every original baseline member remains byte/hash checked. R5complete acceptance and every old closure remain unchanged. A synthetic regression uses the actual nested snapshot layout; the earlier shallow-root fixture's omission is acknowledged.

Authority: the user's direct standing technical-recovery instruction at ace242ee5ea14af284401ad857fc3a49b30e1b58. One R6archive invocation and one native SSDtransfer are permitted. This is the second archive invocation overall, after one failed before any archive creation; only one final archive may be created. Zero new jobs, worker replacements, fitting, reanalysis, scientific changes or broader access. No automatic retry. Preserve any newly produced partial/archive and all original failure records.

Caps unchanged:220800 GPU/21600CPUstage seconds,500MB source/model/control/analysis,2GB live,8GB inclusive; local synthetic tests7200seconds cumulative/fourCPU/8GiB/250MB. Verify the new small package on designated THESIS_SSD before staging; final whole/member verified backup precedes all scientific report reads. Historical studies, AV1 and E12 remain untouched.
