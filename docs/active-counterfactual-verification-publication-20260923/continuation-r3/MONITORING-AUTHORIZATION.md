# ACV0 scheduled monitoring and technical-recovery authority

## New direct user instruction, 23 September 2026

> okay do scheduled checks every 2 hours, and if any TECHNICAL issues arise, I give you full permission to resolve them and resume the jobs. (do not of me to approve)

This is direct advance user authorization to monitor the current ACV0 campaign every two hours and diagnose, implement, validate and execute technical repairs and safe continuations without seeking another approval from the user or the reasoning conversation. It supersedes earlier requirements to return for a new recovery/continuation approval when the issue is purely technical. It does not retrospectively change any original authorization, frozen source, result, failed-attempt history or charge.

## Scope and recovery discipline

The target remains the already authorized ACV0 developmental pilot and its fixed339 logical scientific tasks. Preserve all successful work, original source/data roles/grid/models/seeds/endpoints/analysis and finite compute/storage envelopes. Never repeat successful scientific work, submit duplicate live or ambiguous work, reset charges, tune unfavorable outcomes, add cases or launch another experiment.

For a concrete technical failure, preserve the precise error and all affected artifacts, establish exact controller identity, reconcile live/terminal/ambiguous allocations and authenticate completed outputs. Then make the smallest correct technical repair in a separately named/versioned package outside every executed frozen closure. Run focused synthetic/interface tests within the remaining preparation budget, freeze the repair and its finite recovery contract, record this direct authorization, commit/push with remote hash verification, verify the required small-package SSD backup and resume only the precisely identified unsubmitted or genuinely failed affected work. An already completed job missing only a controller acceptance record is reused, not rerun.

Diagnosed failed-task replacement may be authorized by this new instruction if it is necessary to complete the unchanged logical grid, fits the original cumulative caps including all failures and full remaining-work reservations, and is explicitly enumerated in the new recovery contract. No blind retries, automatic requeue, unlimited repair loop or retry of successful/live/ambiguous work. Do not silently reuse obsolete340-attempt acceptance assumptions if a new authorized failed-attempt replacement increases the actual count: retain the original history and publish exact new counts, provenance and charges in a separate combined view. Scientific denominators still contain each successful logical task once; failed attempts never become observations.

Fail-stop behavior remains appropriate inside frozen controllers. It now triggers agent-led diagnosis/recovery under this authority rather than a redundant approval request. Do not patch a running controller or mutate its approval. A control-only continuation must authenticate old STOPs and preserve them, with a new dated resolution; it cannot simply ignore new STOP files.

No scientific changes, resource-envelope expansion, additional data access, model promotion, AV1, unrelated study or protected payload access is authorized. Use the existing configured runtime and access; do not bypass permission/security controls, extract credentials, alter account security or improvise WSL/filesystem changes. Notify the user only if a genuine unresolved scientific ambiguity, access/security barrier, exhausted finite budget, unavailable required device or other blocker cannot be resolved within this scope. Lost SSH is not evidence of cluster failure: reconnect/read status safely, never infer that research must be relaunched.

## Starting point, not a current-progress claim

Read LAUNCH-20260923.md, IDENTITIES.md, FAULT-AND-SCOPE.md, the frozen control-r3 CONTRACT/BASELINE and all subsequent recovery records before acting. The latest recorded R3 launch receipt commit is `dd02665534341bd0351a9b7fbc50032347158596`. At the historical observation19:19UTC, controller940709/start894795578 was healthy and job304224/collect-fit-505 was RUNNING. This is not live status; read the current cluster ledger.

Checkout: `C:/Users/Chris/thesis-recovery/active-counterfactual-verification-20260923`, branch `active-counterfactual-verification-preparation-20260923`.

Remote run: `/lustreFS/data/superworld/ckontzias/thesis/experiments/active-counterfactual-verification-pilot-v1/run-c2c6fcbe8c41fed2`.

R3 source/control namespaces: `/lustreFS/data/superworld/ckontzias/thesis/snapshots/active-counterfactual-verification-control-r3-4cd769916c5e6f38` and `/lustreFS/data/superworld/ckontzias/thesis/staging/active-counterfactual-verification-control-r3-4cd769916c5e6f38`.

R3 manifest `4cd769916c5e6f38c64e8ded9e058ab281f826cf0ef7dee0ec79840195b93588`; approval `157d796cbe023d2f826b604671f58bb688722963039f6697ec31f89e1059c7a0`. Original R1 scientific manifest `c2c6fcbe8c41fed22d0fde17bcb5f8cd456a74c1cb0d77039f4df94d86e0d468`; worker approval `e1e4b6f6d0e0d1fbb83e89d165be793128f02130647e21a1c12c5892d7d90460`.

R3 resumed with24 successful suppliers,3015 charged GPU-seconds and315 unsubmitted tasks. All historicalR1/R2 charges remain included. Original caps:220800 GPU-seconds,21600 CPU-stage allocation-wall seconds, serial one exact RTX6000 Ada on gpu09, original4CPU/memory/time limits;500MB source/model/control/analysis,2GB live,8GB inclusive, original per-worker caps. Preparation tests consumed916.470seconds of the existing7200second cumulative ceiling through R3; retain four CPU threads,8GiB and250MB preparation bound.

## Completion and notifications

Routine checks inspect only technical control, scheduler, process, seals and resource metadata. Keep scientific outcomes/aggregates unread until complete acceptance and verified designated-SSD final preservation. On completion use the correct authenticated finalization adapter for the actual controller lineage; never call the incompatible old R1 archive entry point, recompute completed science or redo a completed archive. Include all original and recovery evidence in the new-study-only archive, verify the whole file and every member on THESIS_SSD, then publish the complete fixed result and exact accounting. No historical-study rearchive/retransfer or fallback disk. Keep all original failed transfer partials if preservation recovery becomes necessary.

The historical LGP monitor remains paused and unchanged. This new ACV0 monitor should stay quiet for unchanged/ordinary healthy progress and notify on meaningful stage changes, repairs/resumptions, completion or a genuine blocker. Once the current campaign, verified preservation and required report are complete, disable this ACV0 monitor; no follow-up experiment is authorized by this monitoring request.

## Scheduling receipt

Created and read back in the app: `monitor-acv0-and-recover-technical-faults`, named **Monitor ACV0 and recover technical faults**, ACTIVE, every two hours, attached to the current task `019fa7b2-f535-75d0-a393-b189993ff3d3`. Creation timestamp1790191480588milliseconds (2026-09-23 19:24:40.588UTC). The existing `monitor-lgp-rb1-every-two-hours` was independently read back as PAUSED and was not changed. No duplicate ACV0 monitor was present.

The full durable monitor prompt includes this direct authority, exact R3/run/source identities, technical-only observation, bounded diagnosed recovery, unchanged scientific/resource constraints, final preservation/reporting and pause-on-completion instructions. Local scheduled checks require the computer and desktop app to remain running; detached cluster execution itself does not depend on the laptop.
