# ACV0 prelaunch operational fault — no research allocation

The supplied `EXECUTE ACV0` direction identifies implementation `3b2db69e563af89b1ced2149b11c11a1999ba4df` and preservation receipt `b27605ccf7e9097beb02e3d0d638d3e31a175121`. It is recorded as execution direction under standing delegation, **not a new direct user signature**.

## What happened

All local approved hashes matched, including the distinction between the canonical grid digest and serialized file hash. The 706,560-byte source export and all40 transport members authenticated. The designated THESIS_SSD volume was present with323,583,877,120 free bytes.

The new operational helper `operate.py`, outside the approved execution closure, put the full input-binding JSON into its SSH command-line argument. Windows rejected the process creation with `FileNotFoundError(206): The filename or extension is too long`, before WSL or SSH started. The failed operation is preserved in `REMOTE-PREFLIGHT.json` (Unix1790161962.018–1790161962.032). This is a local orchestration defect introduced during this task, not a cluster, model or scientific failure. Remote runtime authentication was not reached.

## Reconciliation and retained state

One narrow read-only reconciliation at cluster Unix1790162039.5248885 confirmed the exact intended snapshot, control and run paths are absent; `squeue` and `sacct` contain no matching `acv0b1-` attempts. There is no live, terminal or ambiguous ACV0 allocation to cancel, retry or charge. GPU allocation0s; CPU-job allocation0s. No research checkpoint or reference payload was opened, no simulator ran, no enabled approval was created, and no controller started. Included sources490/545 remain **not started**.

The original false template, every frozen source member, preparation receipts, earlier outcomes and E12 drafts are unchanged. No alternative namespace, dependency/permission change, research rerun or automatic retry was attempted. The received instruction, local check, failure and read-only reconciliation are preserved alongside this report.

## Narrow recovery needed

Correct only the new launch-operation transport to send bounded metadata/code through SSH stdin instead of an oversized Windows command argument. Then permit authenticated staging and the single campaign launch in the **same** still-absent exclusive paths, with the exact approved scientific package,339-job grid, included tranche and unchanged caps. No scientific re-freeze, extra preflight GPU allocation or research retry is proposed. No such correction or renewed launch attempt has been executed in this turn.
